# PROGRESS — haftalik retro

Har hafta oxirida 15 daqiqa: nima bitdi, nima sekin ketdi, keyingi hafta rejasi. 3-4 qator, sana bilan.

---

## 2026-07-11

- Faza 0 boshlandi: PLAN.md/DATABASE.md/CHECKLIST.md asosida `TASKS.md` yozildi
- Task 1 (monorepo skelet + tooling + CLAUDE.md) bajarildi
- Task 2 (Docker Compose: postgres/redis/minio/nginx + api/worker/web Dockerfile skeletlari) bajarildi — mahalliy Docker yo'q, sinov keyinroq
- Task 3 (Prisma sxema — 34 model, RLS/CHECK/immutable SQL, seed) bajarildi — `prisma validate`/`generate` o'tdi, Prisma 7 chiqqan lekin 6.19.3'ga qotirildi (DATABASE.md klassik `datasource url` uslubiga mos); birinchi migratsiya mahalliy Postgres yo'qligi sababli qo'llanilmadi
- Task 4 (Express API skeleti: app/index, /healthz, AppError+xato-handler, withTenant RLS wrapper, qatlam-buzilish ESLint qoidasi) bajarildi — Vitest+Supertest bilan 13 test yashil, server real ishga tushirilib `/healthz` qo'lda ham tekshirildi (DB/Redis yo'qligida to'g'ri 503 "degraded" qaytardi)
- Task 5 (CI: GitHub Actions lint/format/prisma-validate/test + Sentry ulanish) bajarildi — 5/6 Faza 0 vazifasi tugadi, navbatda commit (har 5 taskda)
- Task 1-5 commit qilindi (`d6ba1b9`) — git identity shu sessiyada birinchi marta sozlandi (global: Hamidullo Tuychibaev)
- GitHub repo ulandi (`Hamidullo/murcha`), default branch `master`→`main`ga o'zgartirildi, `gh` CLI o'rnatildi va autentifikatsiyalandi
- Task 6 (Dizayn skill'lar: Impeccable loyiha ichida, taste-skill 13ta + emilkowalski animatsiya 5ta `npx skills` orqali, Graphify birinchi marta ishga tushirildi — 329 tugun/59 hamjamiyat) bajarildi. Skill fayllari o'zi gitignore (absolyut symlink — boshqa mashinada buziladi), faqat `skills-lock.json` commit qilinadi
- **Faza 0 — 6/6 vazifa bajarildi.** Faqat `docker compose up`ning o'zi sinalmagan (mahalliy Docker yo'q) — qolgan hammasi (lint/test/prisma validate/CI/`/healthz`) tekshirildi
- Faza 1 boshlandi: `TASKS.md` 6 vazifaga bo'lindi (plan-rejimda). Task 1 (auth fondamenti) bajarildi:
  - **RLS kashfiyoti**: login oqimida `company_members`ni `user_id` bilan qidirish kerak (company hali tanlanmagan), lekin joriy policy buni 0 qatorga qisqartirar edi → policy'ga `OR user_id = current_setting('app.user_id')` qo'shildi, `withTenant()` `userId` qabul qiladigan qilindi + yangi `withUserContext()`
  - argon2 parol util, JWT (access, 15 daq) util, `users`/`companies`/`company-members` repository qatlami — hammasi test bilan (24/24 test yashil)
  - Hali HTTP endpoint yo'q (Task 2/3'da) — bu sof fondament
- Task 2 (ro'yxatdan o'tish) bajarildi: `packages/shared` Zod sxemasi (`registerSchema`), `validate()` middleware, `auth` moduli (routes/controller/service — DI kompozitsiya ildizi `auth.routes.js`da), `POST /api/v1/auth/register`. ESLint qatlam qoidasi toraytirildi (faqat `*.controller.js`, `*.routes.js` DI ildizi sifatida istisno). 35/35 test yashil, shu jumladan supertest bilan to'liq integratsiya (register → 201/400/409)
- Task 3 (login + kompaniya tanlash) bajarildi: `withoutTenant()` (global `users` jadvali uchun), `POST /auth/login` (bitta a'zolik → to'g'ridan access token; ko'p a'zolik → 5 daqiqalik `pendingToken` + kompaniyalar ro'yxati), `POST /auth/select-company` (`pendingToken`+`companyId` → access token). `jwt.js`: `signAccessToken`/`signPendingToken`/`verifyToken` (payload'da `type` maydoni bilan ajratiladi). 56/56 test yashil (3 task, oxirgi commitdan beri — hali 5taga yetmagan)
- Task 4 (refresh token rotation + sessiyalar) bajarildi: `SessionsRepository` (Redis, opaque refresh token — JWT emas), httpOnly cookie `murcha_rt` (`sessionId.refreshToken`, path `/api/v1/auth`). `POST /auth/refresh` — rotation; token mos kelmasa (reuse/o'g'irlanish belgisi) sessiya darhol bekor qilinadi. `GET/DELETE /auth/sessions`, `POST /auth/logout` — joriy sessiya cookie orqali "kim so'ramoqda" aniqlanadi (hali alohida RBAC/`requireAuth` kerak emas — Task 5'da boshqa domenlar uchun qo'shiladi). register/login/select-company endi avtomatik sessiya ochadi. 85/85 test yashil, real server ishga tushirilib tekshirildi
- Task 5 (RBAC middleware) bajarildi: `requireAuth` (JWT access token → `req.auth = {userId, companyId, roleId}`, business modullar Faza 2+ shundan `withTenant()` chaqiradi), `requirePermission(code)` (`role_permissions` orqali, `withTenant` ichida). `RolesRepository.hasPermission`, `CompaniesRepository.findById` qo'shildi. `GET /api/v1/auth/me` — birinchi `requireAuth` bilan himoyalangan endpoint (namuna + amaliy foyda: frontend token olgach profil so'raydi). 102/102 test yashil
- Foydalanuvchi so'rovi: fazani oxirigacha (Task 6) yetkazib keyin commit+push qilinadi — 5-task qoidasidan bu safar chetga chiqildi
- Task 6 (rate-limit + brute-force himoya) bajarildi: `rateLimit()` middleware (IP bo'yicha, Redis INCR+EXPIRE — login'da 20 so'rov/5 daq), `LoginAttemptsRepository` (telefon bo'yicha, 5 xato urinishdan keyin 15 daqiqa blok, `AuthService.login()` ichida — muvaffaqiyatli login hisobni nolga tushiradi), `TooManyRequestsError` (429). 115/115 test yashil, real server bilan qo'lda tekshirildi
- **Faza 1 — 6/6 vazifa bajarildi.** Barcha auth oqimi (register/login/select-company/refresh/logout/sessions/me/RBAC/rate-limit) yozilgan va mock Prisma/Redis bilan to'liq testlangan. Haqiqiy Postgres'da RLS izolyatsiyasi hali tasdiqlanmagan (mahalliy DB yo'q — Faza 0'dagi bilan bir xil cheklov)
- Keyingi: commit + push (foydalanuvchi so'rovi bo'yicha), so'ng Faza 2 — Mahsulot katalogi
- Faza 2 boshlandi: `TASKS.md` 8 vazifaga bo'lindi (plan-rejimda) — Prisma sxemasi bu faza uchun kerakli modellarni Faza 0'da o'z ichiga olgani aniqlandi, shu sababli "birliklar CRUD" alohida vazifa emas (tizim birliklari `seed.js`da urug'langan). Task 1 (skladlar + kategoriyalar) bajarildi: `warehouses` va `categories` modullari (repository→service→controller→routes, Faza 1'dagi `auth`/`companies` qolipi bilan bir xil), RBAC — mavjud `warehouse.manage`/`products.manage` ruxsatlaridan foydalanildi (yangi permission kod qo'shilmadi). Qaror: GET (ro'yxat/bitta) faqat `requireAuth` bilan himoyalangan, `requirePermission` faqat yozish (POST/PATCH) endpointlarida — sklad/kategoriya uchun alohida "*.view" kodi yo'q, o'qish barcha hodimlarga ochiq. Kategoriya `parentId` — o'z-o'ziga ota bo'lish taqiqlangan, RLS orqali boshqa kompaniya kategoriyasiga bog'lash bloklanadi. 152/152 test yashil (37 yangi), lint/format toza, `prisma validate` o'tdi
- Task 2 (mahsulot asosiy CRUD) bajarildi: `products` moduli (Faza 1 qolipi bilan bir xil). Biznes tekshiruvlar: SKU noyobligi (`@@unique([companyId, sku])` — ilova darajasida oldindan tekshiriladi, DB xatosini catch qilish o'rniga `registerCompany()`dagi kabi), `baseUnitId` mavjudligi (yangi minimal `units` moduli — faqat `findById`, to'liq birliklar CRUD keyinga qoldirilgan), `categoryId` mavjudligi (Task 1'dagi `CategoriesRepository` qayta ishlatildi). Soft-delete: `DELETE /api/v1/products/:id` jismoniy o'chirmaydi — `status:"archived"` + `deletedAt` belgilaydi (mahsulot boshqa jadvallarda ishlatilishi mumkin). Ro'yxat `deletedAt:null` bilan filtrlanadi, `getById` arxivlangan mahsulotni ham qaytaradi. 179/179 test yashil (27 yangi), lint/format toza, `prisma validate` o'tdi
- Commit + push (foydalanuvchi so'rovi: "task 2 ohirida push qilamiz") — `d096fcc`
- Task 3 (o'ram-birlik konvertatsiyasi + shtrix-kodlar) bajarildi: `ProductUnitsRepository`/`ProductBarcodesRepository` (`products` moduli ichida, alohida modul emas — `product_units`/`product_barcodes` `company_id` ustunisiz bola-jadvallar, `ProductsService`ning o'zi kengaytirildi, TASKS.md'dagi rejaga mos). `product_units`da RLS yo'q (rls.sql: bola-jadval) — servis har doim avval `ProductsRepository.findById` orqali mahsulot egaligini tekshiradi, keyin bola-jadvalga murojaat qiladi. Qoidalar: asosiy birlikni qayta qo'shib bo'lmaydi, bitta birlik bitta mahsulotga faqat bir marta qo'shiladi (`@@unique([productId, unitId])` oldindan tekshiriladi), shtrix-kod kompaniya bo'yicha noyob (`@@unique([companyId, barcode])`). Nested endpointlar: `POST/GET /products/:id/units`, `DELETE /products/:id/units/:unitId`, xuddi shunday `/barcodes` uchun. 212/212 test yashil (33 yangi), lint/format toza, `prisma validate` o'tdi
- Task 4 (narx turlari + narx tarixi) bajarildi: `price-types` — to'liq mustaqil modul (Faza 1 qolipi, `warehouses`ga o'xshash CRUD, `isDefault` bitta kompaniyada faqat bitta narx turida bo'lishi — yangisi belgilanganda avvalgisi `unsetDefault()` bilan o'chiriladi). `product-prices` — TASKS.md'da alohida modul sifatida rejalashtirilgan (Task 3'dagi "servis kengaytirish"dan farqli), `products` papkasida, lekin o'z repository/service/controller/routes bilan; `Router({ mergeParams: true })` orqali `/api/v1/products/:id/prices` ostiga joylashtirildi. Narx immutable — UPDATE/DELETE yo'q, faqat `create` (yangi `validFrom` bilan yangi qator). `GET .../prices/current` — Prisma `distinct: ["priceTypeId"]` + `orderBy` (Postgres `DISTINCT ON` ekvivalenti) orqali har narx turi uchun eng oxirgi narxni qaytaradi. 245/245 test yashil (33 yangi), lint/format toza, `prisma validate` o'tdi
- Task 5 (mahsulot variantlari) bajarildi: `product-variants` — Task 4'dagi `product-prices` bilan bir xil uslub (mustaqil repository/service/controller, `Router({ mergeParams: true })` orqali `/api/v1/products/:id/variants` ostida). `product_variants` — RLS'siz bola-jadval (Task 3/4'dagi kabi, ota mahsulot orqali izolyatsiya). DB'da variant SKU'ga unique constraint yo'q — shu sababli ilova darajasida ham noyoblik tekshiruvi qo'shilmadi (haqiqiy sxema cheklovidan ortiq qoida o'ylab topilmadi). Soft-delete — `Product`dagi kabi, lekin faqat `deletedAt` (status maydoni yo'q). Faza 2 asosiy backend qismi (Task 1-5) shu bilan tugadi — qolgan Task 6-8 (rasm yuklash, katalog UI) alohida infratuzilma/frontend ishi. 265/265 test yashil (20 yangi), lint/format toza, `prisma validate` o'tdi
- Task 6 (rasm yuklash) bajarildi: yangi paketlar (`minio`, `bullmq`, `sharp`, `multer`) — `sharp`ning native build skripti pnpm supply-chain siyosatida bloklanib, `msgpackr-extract`ni (bullmq bog'liqligi) `pnpm-workspace.yaml`ning `allowBuilds`iga qo'shishga to'g'ri keldi (`argon2`/`prisma`dagi Faza 0 holatiga o'xshash). `lib/minio.js` (`ensureBucket()` ilova/worker ishga tushganda chaqiriladi), `lib/queue.js` (BullMQ o'z Redis ulanishini talab qiladi — `maxRetriesPerRequest:null`, umumiy `lib/redis.js` bilan bo'lishilmaydi). `product-images` moduli — Task 4/5 bilan bir xil uslub (`Router({mergeParams:true})`, `/api/v1/products/:id/images`), lekin biznes-mantiqda muhim farq: MinIO (tarmoq I/O) chaqiruvlari Prisma tranzaksiyasi ichida ushlab turilmaydi — `auth.service`dagi Redis sessiya patterniga o'xshab, DB tekshiruvi qisqa alohida tranzaksiyalarda. Yuklash: birinchi rasm avtomatik `isMain:true`, `putObject()` MinIO'ga yozadi, so'ng BullMQ navbatiga thumbnail generatsiya joby qo'shiladi (`worker.js` — endi haqiqiy `sharp`+MinIO worker, Faza 0'dagi skelet o'rniga). O'chirish — avval MinIO'dan (original+thumb), keyin DB qatori. `apps/api/src/worker.js`/`index.js` test qilinmagan (entrypoint fayllar, `index.js` bilan bir xil konventsiya) — faqat ichidagi `lib/` funksiyalar (`ensureBucket`) test qilingan. 292/292 test yashil (27 yangi), lint/format toza, `prisma validate` o'tdi. Haqiqiy MinIO/BullMQ ulanishi mahalliy infratuzilma yo'qligi sababli hali sinalmagan
- Faza 2 asl Task 7 ("Katalog UI: ro'yxat+qidiruv") oldidan `apps/web` hali Faza 0 statik placeholder ekani aniqlandi — Vue/Vite/Tailwind/shadcn-vue/Pinia/TanStack Query hech biri yo'q edi. Plan-rejimda qaror qilindi: TASKS.md qayta raqamlandi (9 vazifa) — yangi Task 7 = frontend skelet + auth ekranlari, eski Task 7/8 → Task 8/9 (katalog ro'yxat, mahsulot forma) siljidi.
- Task 7 (frontend skelet + auth) bajarildi: `apps/web` to'liq qayta qurildi (Vue 3.5 + Vite + Tailwind 4 CSS-first konfiguratsiya + shadcn-vue kod sifatida — `Button`/`Input`/`Label`/`Card`, Reka UI asosida keyingi tasklar uchun tayyor + Pinia (`auth.store.js`, access token faqat xotirada — XSS xavfsizligi, refresh httpOnly cookie orqali) + TanStack Query o'rnatildi + Vue Router (`beforeEach` guard, faqat UX — RBAC backendda). API klient (`src/api/client.js`) 401'da bitta marta `refresh()` bilan qayta urinadi. `packages/shared`dagi `loginSchema` frontend formada ham ishlatiladi (DRY). `eslint-plugin-vue` + `vue-eslint-parser` qo'shildi (`eslint.config.js`), `.claude/launch.json` yaratildi (`web`/`api` preview konfiguratsiyalari). Claude Browser'da tekshirildi: login sahifa render, client-side Zod validatsiya, tarmoq so'rovi to'g'ri endpoint/bodyga yuboriladi, route guard (`/` → `/login` tokensiz), backend 500 xatosi (DB/Redis yo'qligi) UI'da "Server xatosi" sifatida grasefully ko'rsatiladi — hammasi kutilganidek. To'liq login→dashboard oqimi haqiqiy Postgres/Redis muhitida hali tasdiqlanmagan (Faza 0/1/2'dagi bilan bir xil cheklov). `pnpm format` ishga tushirilganda 4 ta mendan tegilmagan fayl (PLAN.md, docker-compose.yml, branding/design-prompts.md, DATABASE.md) ham qayta formatlandi — bu o'zgarishlar commit qilinmadi (faqat Task 7'ga tegishli fayllar stage qilindi). nginx.conf'da `/api` prefiksini noto'g'ri kesib tashlash xatosi payqaldi (Faza 0'dan qolgan, Docker sinalmagani uchun oldin sezilmagan) — alohida background task sifatida belgilandi (`task_ad5d6e3b`), bu sessiya doirasidan tashqari
- Task 8 (katalog UI: ro'yxat + qidiruv/filter) bajarildi. Backend: `products.repository.list()` `search`/`categoryId` filtrlarni qabul qiladigan bo'ldi (`nameUz: {contains, mode:"insensitive"}` — Postgres'da `ILIKE`ga compile bo'ladi), natijaga `category`/`baseUnit` `include` qilindi (frontend N+1 so'rovsiz ko'rsatishi uchun). Yangi `prisma/search.sql` (`CREATE EXTENSION pg_trgm` + GIN indeks `name_uz`ga) — `pnpm db:search` skripti bilan qo'llaniladi (rls.sql/immutable.sql bilan bir xil pattern, hali ishga tushirilmagan — Postgres yo'q). `GET /api/v1/products` endi query parametrlarni Zod bilan tekshiradi (`listProductsQuerySchema`). Frontend: `ProductListPage.vue` — qidiruv input (300ms debounce, qo'lda `setTimeout`, VueUse hali kerak emas) + kategoriya select + jadval (TanStack Query `useQuery`, loading/error/bo'sh holatlar). `DashboardPage.vue` o'chirildi, `/` route'i endi to'g'ridan-to'g'ri katalogni ko'rsatadi (route nomi `dashboard`→`products`). Claude Browser'da tekshirildi: brauzer konsolida Pinia/router instance'larga to'g'ridan-to'g'ri murojaat qilib (auth guard'ni chetlab o'tib) sahifa haqiqiy render bo'lishini tasdiqladim — layout, qidiruv input, kategoriya select, loading→error holat almashinishi hammasi kutilganidek ishladi (401 xato — soxta token, DB yo'q). Shu tekshiruv paytida real muammo topildi: TanStack Query'ning standart 3x avtomatik retry'i `apiFetch`ning o'z 401-refresh-retry logikasi bilan qo'shilib so'rov shtormiga olib kelayotgan edi — `main.js`da `VueQueryPlugin`ga `defaultOptions.queries.retry:false` qo'shib tuzatildi. 297/297 backend test yashil (5 yangi), lint/format toza, `prisma validate` o'tdi, frontend build muvaffaqiyatli
- Task 9 (katalog UI: mahsulot forma) bajarildi — Faza 2ning oxirgi vazifasi. Backend qo'shimchalari (Task 9 uchun zarur bo'lgani aniqlandi): `units` moduli endi to'liq (`GET /api/v1/units` — RLS `company_id IS NULL OR ...` orqali tizim+kompaniya birliklari qaytadi, forma uchun asosiy birlik tanlash kerak edi), `product-images`ga `GET /products/:id/images/:imageId/url` — MinIO `presignedGetObject()` orqali 15 daqiqalik vaqtinchalik URL (bucket ochiq emas, `<img>` to'g'ridan-to'g'ri MinIO manziliga ishora qila olmaydi). Frontend: `ProductFormPage.vue` — bitta komponent yaratish (`/products/new`) va tahrirlash (`/products/:id/edit`) ikkalasini ham boshqaradi (`route.params.id` bo'yicha), asosiy maydonlar formasi (`createProductSchema`/`updateProductSchema`) + faqat tahrirlash rejimida ko'rinadigan uchta bo'lim: Narxlar (joriy narxlar ro'yxati + narx turi/qiymat/valyuta qo'shish), Variantlar (ro'yxat + nom bilan qo'shish/o'chirish), Rasmlar (fayl yuklash, asosiy qilish, o'chirish, presigned URL orqali ko'rsatish). `ProductListPage.vue`ga "Yangi mahsulot" tugmasi va qator bosilganda tahrirlashga o'tish qo'shildi. Kichik sifat tuzatishi: `createProductSchema`dagi `sku`/`nameUz`/`baseUnitId` uchun standart Zod inglizcha xabarlar o'rniga o'zbekcha xabar qo'shildi (loginSchema'dagi bilan bir xil uslub) — Claude Browser'da submit qilib ko'rgach payqaldi. Claude Browser'da tekshirildi: yaratish formasi render, maydon-bo'yicha client validatsiya (bo'sh/qisman to'ldirilgan holatlar to'g'ri ishladi), va muhim xavfsizlik tasdiqi — fon so'rovlari (kategoriya/birlik ro'yxati) yaroqsiz token bilan 401 qaytarganda, `apiFetch`ning refresh-retry logikasi ham muvaffaqiyatsiz bo'lib, auth store avtomatik tozalanib foydalanuvchi `/login`ga qaytarildi (kutilgan, to'g'ri xavfsizlik xatti-harakati). Tahrirlash rejimidagi uch bo'lim (narx/variant/rasm) haqiqiy mahsulot ID'siz vizual tasdiqlanmadi — bu haqiqiy Postgres kerak bo'lgan joy. 304/304 backend test yashil (7 yangi — units + presigned URL), lint toza (24 ta jsdoc ogohlantirish, xato emas — mavjud konventsiyaga mos), frontend build muvaffaqiyatli. **Faza 2 — 9/9 vazifa bajarildi**, faqat "Natija" (100 mahsulotli real demo) haqiqiy Postgres/Redis/MinIO muhitida hali tasdiqlanmagan
- Task 3 (o'ram-birlik konvertatsiyasi + shtrix-kodlar) bajarildi: `ProductUnitsRepository`/`ProductBarcodesRepository` (`products` moduli ichida, alohida modul emas — `product_units`/`product_barcodes` `company_id` ustunisiz bola-jadvallar, `ProductsService`ning o'zi kengaytirildi, TASKS.md'dagi rejaga mos). `product_units`da RLS yo'q (rls.sql: bola-jadval) — servis har doim avval `ProductsRepository.findById` orqali mahsulot egaligini tekshiradi, keyin bola-jadvalga murojaat qiladi. Qoidalar: asosiy birlikni qayta qo'shib bo'lmaydi, bitta birlik bitta mahsulotga faqat bir marta qo'shiladi (`@@unique([productId, unitId])` oldindan tekshiriladi), shtrix-kod kompaniya bo'yicha noyob (`@@unique([companyId, barcode])`). Nested endpointlar: `POST/GET /products/:id/units`, `DELETE /products/:id/units/:unitId`, xuddi shunday `/barcodes` uchun. 212/212 test yashil (33 yangi), lint/format toza, `prisma validate` o'tdi
- Task 2 (mahsulot asosiy CRUD) bajarildi: `products` moduli (Faza 1 qolipi bilan bir xil). Biznes tekshiruvlar: SKU noyobligi (`@@unique([companyId, sku])` — ilova darajasida oldindan tekshiriladi, DB xatosini catch qilish o'rniga `registerCompany()`dagi kabi), `baseUnitId` mavjudligi (yangi minimal `units` moduli — faqat `findById`, to'liq birliklar CRUD keyinga qoldirilgan), `categoryId` mavjudligi (Task 1'dagi `CategoriesRepository` qayta ishlatildi). Soft-delete: `DELETE /api/v1/products/:id` jismoniy o'chirmaydi — `status:"archived"` + `deletedAt` belgilaydi (mahsulot boshqa jadvallarda ishlatilishi mumkin). Ro'yxat `deletedAt:null` bilan filtrlanadi, `getById` arxivlangan mahsulotni ham qaytaradi. 179/179 test yashil (27 yangi), lint/format toza, `prisma validate` o'tdi

## 2026-07-12

- Faza 3 (sklad operatsiyalari) boshlandi: plan-rejimda kelishildi (CLAUDE.md — qoldiq tranzaksiyasi murakkab joy). Barcha kerakli Prisma modellar (`Stock`/`WarehouseDoc`/`WarehouseDocItem`/`StockMovement`/`PurchaseOrder`/`InventoryCount`/`DocCounter`) Faza 0'da tayyor — sxema o'zgarmaydi. TASKS.md 7 vazifaga bo'lindi. Arxitektura qarorlari: `Stock` qulflash — Prisma `upsert` + `{ increment }` (Postgres'da bitta `INSERT...ON CONFLICT DO UPDATE`, alohida `SELECT...FOR UPDATE` shart emas); deadlock oldini olish — transfer'da qulflar `(warehouseId, productId, variantId, batchId)` bo'yicha leksikografik tartibda olinadi; o'rtacha tannarx alohida ustunsiz, `stock_movements.cost_price`dan so'rov vaqtida hisoblanadi (sxema muzlatilgan).
- Task 1 (warehouse-docs qoralama) bajarildi: `warehouse-docs` moduli (Faza 1/2 qolipi bilan bir xil — repository/service/controller/routes/schemas+testlar). `createWarehouseDocSchema` — Zod `superRefine` bilan `transfer`da `toWarehouseId` shart/boshqa turlarda taqiqlangan tekshiruvi. Hujjat raqami: `doc_counters`ga Prisma `upsert({update:{counter:{increment:1}}}）` — Postgres'da atomik `INSERT...ON CONFLICT`, race yo'q (`KIR/CHIQ/SPIS/KOCH-YYYY-NNNNN`). Qator qo'shishda `qtyBase` hisoblanadi: birlik mahsulotning asosiy birligiga teng bo'lsa `qty` o'zi, aks holda `ProductUnit.factor` orqali (Task 3'dagi kabi, ulanmagan birlik `ValidationError`). Faqat `status:"draft"` hujjat tahrirlanadi (`ConflictError` boshqa holatda) — tasdiqlash/storno (`stock`/`stock_movements`) keyingi vazifalarda. `InsufficientStockError` allaqachon Faza 0'da `lib/errors.js`da mavjud ekan (yangi qo'shish shart bo'lmadi). 334/334 backend test yashil (30 yangi), lint/format toza.
- Task 2 (tasdiqlash oqimi) bajarildi: yangi `stock` moduli (`StockRepository`/`StockMovementsRepository` — to'liq o'qish API'si Task 4'da qo'shiladi, hozircha faqat `confirm()` uchun kerakli yozish/qidirish metodlari). `WarehouseDocsService.confirm(docId)` — status draft ekanini tekshiradi, har qatordan hujjat turiga qarab signed harakat(lar) hosil qiladi (receipt +, issue/writeoff -, transfer ikkala skladga - va +), barcha harakatlarni `(warehouseId, productId, variantId, batchId)` bo'yicha leksikografik tartiblab, ketma-ket qo'llaydi. Muhim kashfiyot: Postgres standart bo'yicha UNIQUE constraint'da NULL ustunlarni "teng emas" deb hisoblaydi — `stock`ning `UNIQUE(warehouseId, productId, variantId, batchId)`i variant/partiyasiz mahsulotlarda (ikkalasi ham NULL) Prisma `upsert()`ning tayangan `INSERT...ON CONFLICT`ini ishlatmay, har tasdiqlashda yangi qator yaratardi. Yangi `prisma/stock.sql` shu constraint'ni `NULLS NOT DISTINCT`ga o'zgartiradi (checks.sql/immutable.sql/search.sql bilan bir xil pattern — birinchi migratsiya generatsiya qilingach qo'lda qo'shiladi). Manfiy qoldiq: oldindan tekshiruv (`StockRepository.findOne` — joriy qoldiq + delta < 0 bo'lsa `InsufficientStockError`) — bu loyihaning "DB xatosini catch qilish o'rniga ilova darajasida oldindan tekshirish" konvensiyasiga mos (Faza 2 Task 2'dagi SKU tekshiruvi kabi); DB CHECK (`checks.sql`) haqiqiy race-condition'lar uchun oxirgi himoya sifatida qoladi. 347/347 backend test yashil (13 yangi), lint/format toza.
- Task 3 (storno) bajarildi: `WarehouseDocsService.cancel(docId)` — faqat `status:"confirmed"` hujjatni bekor qiladi (`ConflictError` boshqa holatda), hujjat/item'ning o'zi o'zgarmaydi (immutable — DATABASE.md printsipi), teskari `StockMovement` qatorlari yoziladi (o'sha `docId`ga bog'liq). Refaktoring: `confirm()`dagi qulflash+harakat qo'llash tsikli umumiy `#applyOperations()`ga chiqarildi, `#buildOperations(doc, item, direction)` endi `direction: 1 | -1` qabul qiladi (`confirm` — asl yo'nalish, `cancel` — teskari) — Task 2'da shu refaktoring rejalashtirilgan edi (plan hujjati: "storno... xuddi shu qulflash texnikasi"), endi haqiqiy ikkinchi chaqiruvchi paydo bo'lgach amalga oshirildi. Storno ham `InsufficientStockError`ga uchrashi mumkin (masalan kirim hujjati storno qilinayotganda, lekin qoldiq allaqachon boshqa chiqim orqali sarflangan bo'lsa) — bu kutilgan, to'g'ri xatti-harakat. 355/355 backend test yashil (8 yangi), lint/format toza. Faza 3 — 3/7 vazifa (qoralama CRUD + tasdiqlash + storno tugadi, navbatda `stock` moduli o'qish API'si).
- Task 4 (stock moduli) bajarildi: `stock` moduli o'qish API'si to'liq bo'ldi — `GET /api/v1/stock` (sklad/mahsulot filtri), `GET /api/v1/stock/low` (`minQty` belgilangan qatorlardan `quantity<=minQty`), `GET /api/v1/stock/average-cost` (og'irlashtirilgan o'rtacha, `SUM(qty*costPrice)/SUM(qty)` — qty>0 harakatlardan). Ikkita texnik cheklov Prisma query API bilan ifodalanmagani aniqlandi: (1) ikki ustunni taqqoslash (`quantity<=minQty`) Prisma `where`da yo'q — `onlyTracked` filtri bilan qisqartirilgan ro'yxat oling, keyin JS'da filtrlanadi; (2) `SUM(qty*costPrice)` kabi ustunlar ko'paytmasi aggregatsiyasi ham Prisma `aggregate`da yo'q — mos harakatlar ro'yxati olinib JS'da reduce qilinadi. Ikkalasi ham MVP ko'lamida (kompaniya/sklad darajasida qator soni cheklangan) xavfsiz, real Postgres agregatsiyaga o'tish keyinroq kerak bo'lsa alohida optimallashtirish. Barcha endpoint faqat `requireAuth` — yozish yo'q, `warehouse-docs.confirm/cancel` orqali amalga oshadi. 368/368 backend test yashil (13 yangi), lint/format toza. Faza 3 — 4/7 vazifa.
- Task 5 (purchase orders) bajarildi: `purchase-orders` moduli — PO CRUD (raqamlash `PO-YYYY-NNNNN`, o'z `doc_counters.docType:"purchase_order"` ketma-ketligi), item qo'shish/o'chirish (faqat draft). `receive(poId, dto)` — muhim arxitektura qarori: `WarehouseDocsService`ga (to'liq servisga) DI orqali bog'lanish o'rniga `WarehouseDocsRepository`ning o'ziga bog'landi — sabab, `WarehouseDocsService`ning har metodi o'z `withTenant()` tranzaksiyasini ochadi, shuning uchun uni boshqa servis ichidan chaqirish ikkita mustaqil (atomik bo'lmagan) tranzaksiyaga olib kelardi. Repository darajasida bog'lanish esa PO tekshiruvi + kirim hujjat/qator yaratish + `qtyReceived` yangilash + PO status yangilashning barchasi BITTA `withTenant()` tranzaksiyasida bo'lishini ta'minlaydi (qisman muvaffaqiyatsizlikda hech narsa commit bo'lmaydi). Kirim hujjati `status:"draft"` yaratiladi — stock/stock_movements yangilanishi qasddan shu vazifaga kirmaydi, mavjud `POST /warehouse-docs/:id/confirm` orqali alohida bosqichda tasdiqlanadi (ko'rib chiqish imkoniyati). Qisman yetkazish qo'llab-quvvatlanadi (`PurchaseOrderItem.qtyReceived` — har `receive()` chaqiruvida qolgan miqdordan oshsa `ValidationError`, barcha itemlar to'liq qabul qilinganda PO statusi avtomatik `received`ga, aks holda `partially_received`ga o'tadi). 390/390 backend test yashil (22 yangi), lint/format toza. Faza 3 — 5/7 vazifa (qoldi: invariant/race-condition testlar, frontend).
- Task 6 (invariant + race-condition testlar) bajarildi: `warehouse-docs.invariant.test.js` — mahalliy Postgres yo'qligi sababli (takroriy cheklov) xotirada `stock`/`stock_movements`/`warehouse_docs` fake DB yaratildi, lekin **mock qilingan repository emas** — real `WarehouseDocsService`/`StockRepository`/`StockMovementsRepository` kodi shu fake DB ustida ishlaydi, fake `stock.upsert` esa `checks.sql`dagi `stock_quantity_check`ni ham takrorlaydi (manfiy qoldiqda xato otadi). Invariant test: kirim+chiqim+ko'chirish+storno ketma-ketligidan keyin `SUM(stock_movements.qty) = stock.quantity` tekshiriladi (ikkala sklad uchun). Race-condition test — qiziqarli texnik yechim: ikkita parallel `confirm()` chaqiruvi bir xil boshlang'ich qoldiqni ko'rishini kafolatlash uchun sun'iy to'siq (barrier promise) `stock.findUnique`ga qo'yildi — bu real Postgres'dagi ikkita mustaqil tranzaksiyaning bir xil qatorni READ COMMITTED bosqichida o'qishi bilan ekvivalent. Natija: JS bir oqimli (single-threaded) bo'lgani uchun ikkinchi `stock.upsert` chaqiruvi birinchisi commit qilgandan keyin ishlaydi va to'g'ri rad etiladi (`checks.sql` "oxirgi himoya" ekanini isbotlaydi, ilova darajasidagi oldindan tekshiruv race darchasini to'liq yopmasa ham). Test 5 marta ketma-ket barqaror o'tdi (flaky emas). 392/392 backend test yashil (2 yangi).
- Task 7 (frontend) bajarildi — Faza 3ning oxirgi vazifasi. Yangi API klientlar (`warehouses.api.js`, `warehouse-docs.api.js`, mavjud `products.api.js`/`units.api.js` qayta ishlatildi). `WarehouseDocListPage.vue` — turi/holat/sklad filtri (Faza 2'dagi `ProductListPage.vue` bilan bir xil uslub). `WarehouseDocFormPage.vue` — ikki rejimli (Faza 2'dagi `ProductFormPage.vue` naqshi): yaratish (turi/sklad/qabul-sklad — `transfer`da shartli maydon), tahrirlash (qatorlar jadvali + qo'shish formasi faqat `draft`da, "Tasdiqlash"/"Bekor qilish" tugmalari holatga qarab). `AppLayout.vue`ga navigatsiya (`Katalog`/`Sklad hujjatlari`) qo'shildi — ilgari umuman nav yo'q edi. Claude Browser'da tekshirildi: ro'yxat/yaratish sahifalari render bo'ldi, `transfer` turi tanlanganda "Qabul qiluvchi sklad" maydoni shartli ko'rinishi ishladi, client-side Zod validatsiya (`createWarehouseDocSchema`ning `superRefine`i) ikkala xato xabarini to'g'ri ko'rsatdi. Shu tekshiruv paytida real bag topildi: hujjat yuklanish xatosida (`isError`) sahifa **bo'sh qolar edi** (na "Yuklanmoqda…", na xato xabari) — `isDocError` holati va xabar qo'shib tuzatildi. `window.fetch`ni vaqtincha almashtirib xayoliy hujjat ma'lumoti bilan tahrirlash sahifasi (qatorlar jadvali, qo'shish formasi, Tasdiqlash/O'chirish tugmalari) ham tasdiqlandi. `pnpm build` muvaffaqiyatli. 392/392 backend test yashil, lint/format toza.
- **Faza 3 — 7/7 vazifa bajarildi.** Kirim/chiqim/spisaniye/ko'chirish hujjatlari (tasdiqlash+storno), atomik qoldiq hisobi, stock/o'rtacha-tannarx/low-stock API, purchase orders, invariant+race-condition testlar, frontend UI — barchasi yozilgan va testlangan. Haqiqiy Postgres/RLS/qulflash bilan hali tasdiqlanmagan (Faza 0/1/2'dagi bilan bir xil infratuzilma cheklovi) — CHECKLIST.md'da ochiq izoh sifatida qoladi.

## Faza 4 — Shtrix-kod, Excel, inventarizatsiya

- Faza 4 boshlandi: plan-rejimda kelishildi (bir necha mustaqil subsistema — shtrix-kod, Excel, inventarizatsiya — texnik qarorlar oldindan aniqlanishi kerak edi). TASKS.md 8 vazifaga bo'lindi. Prerequisite aniqlandi: `Counterparty` modeli Faza 0'dan bor edi, lekin hech qanday CRUD moduli yo'q edi (na PO'da yetkazib beruvchi tanlash, na Excel kontragent import/export mumkin) — shu sabab Task 1 qilib qo'shildi. Arxitektura qarorlari: shtrix-kod skaner uchun qo'shimcha kutubxonasiz brauzer nativ `BarcodeDetector` API (kamera) + USB skaner klaviatura sifatida (matn input+Enter, kutubxona shart emas); yorliq chop etish uchun `jsbarcode` (frontend, ~20KB); Excel uchun `exceljs` (backend, native bog'liqlik yo'q) — export sinxron, import BullMQ orqali asinxron (1000 qatorli fayl uchun so'rov timeout xavfi), natija BullQ jobning o'zida saqlanadi (yangi DB jadval kerak emas, sxema muzlatilgan); inventarizatsiya tasdiqlashda tuzatish hujjati darhol `confirmed` yaratiladi (PO'dagi kabi draft emas — sanoqning o'zi "haqiqat"ni aniqlagan).
- Task 1 (counterparties CRUD) bajarildi: `counterparties` moduli — standart CRUD (Faza 1-3'dagi `warehouses` qolipi bilan bir xil: repository/service/controller/routes/schemas+testlar). Yangi RBAC kod `counterparties.manage` (`seed.js`ga qo'shildi, owner rolga avtomatik beriladi). Soft-delete — `Product`dagi kabi (`isActive:false`+`deletedAt`, jismoniy o'chirish yo'q — kontragent `warehouse_docs`/`purchase_orders`/`debt_movements`da ishlatilgan bo'lishi mumkin). `list()` — `type`/`search` (nom bo'yicha `ILIKE`) filtri, `deletedAt:null` doim. 408/408 backend test yashil (16 yangi), lint/format toza.
- Task 2 (shtrix-kod qidiruv) bajarildi: `GET /api/v1/products/by-barcode/:barcode` — mavjud `ProductBarcodesRepository.findByBarcode` (Faza 2'dan) qayta ishlatildi, `ProductsService.getByBarcode()` shtrix-kod → mahsulot ID → to'liq mahsulot ma'lumotini qaytaradi (arxivlangan mahsulot topilmadi hisoblanadi). `product_barcodes`da RLS bor (`company_id` ustuni bilan, `product_units`dan farqli) — boshqa kompaniya shtrix-kodi hech qachon qaytmaydi, qo'shimcha tekshiruv shart emas. Route joylashuvi: `/by-barcode/:barcode` `/:id`dan oldin — funksional zarurat emas (segment soni farqli, konflikt yo'q), faqat o'qish tartibi uchun. 413/413 backend test yashil (5 yangi), lint/format toza.
- Task 3 (Excel export) bajarildi: yangi `exports` moduli — `GET /api/v1/exports/products|stock|counterparties`, `exceljs` bilan sinxron `.xlsx` generatsiya (repository darajasida hech qanday yangi so'rov yo'q — mavjud `ProductsRepository`/`StockRepository`/`CounterpartiesRepository.list()` qayta ishlatildi). Yangi backend bog'liqlik `exceljs` — native qism yo'q, `pnpm-workspace.yaml`ga qo'shimcha `allowBuilds` shart bo'lmadi. Test uslubi: `exceljs`ni mock qilish o'rniga haqiqiy kutubxona bilan buffer generatsiya qilinib, keyin `workbook.xlsx.load(buffer)` bilan orqaga o'qib katakchalar tekshirildi — bu haqiqiy `.xlsx` formatini (mock emas) tasdiqlaydi. 420/420 backend test yashil (7 yangi), lint/format toza.
- Task 4 (Excel import) bajarildi: yangi `imports` moduli + `apps/api/src/worker.js`ga ikkinchi BullMQ worker qo'shildi (`excel-import` navbati, mavjud `product-image-thumbnail` bilan bir jarayonda). Oqim: `POST /api/v1/imports/:type` fayl (multer, xotirada) MinIO'ga yoziladi (`imports/{companyId}/{uuid}.xlsx`, `product-images`dagi bilan bir xil "tarmoq I/O tranzaksiyadan tashqarida" pattern), BullMQ job navbatga qo'yiladi — javob darhol `202 {jobId}`. `GET /api/v1/imports/:jobId` — BullMQ'ning o'z `Job.getState()`/`returnvalue`idan holat+hisobot o'qiydi (yangi DB jadval kerak emas, sxema muzlatilgan); `job.data.companyId` so'ragan foydalanuvchi kompaniyasiga mos kelmasa `NotFoundError` — BullMQ/Redis'da RLS yo'qligi uchun tenant-izolyatsiya shu yerda qo'lda ta'minlanadi. Har qator o'z alohida `withTenant` tranzaksiyasida qayta ishlanadi (`import-processors.js`: `importProductRow`/`importStockRow`/`importCounterpartyRow`) — bitta qatorning xatosi qolganlarini to'xtatmaydi, natija `{total,succeeded,failed,errors:[{row,message}]}`.
  **Muhim arxitektura qarori — qoldiq (`stock`) import**: to'g'ridan-to'g'ri `Stock.quantity`ga UPDATE qilinmadi (CLAUDE.md: "qoldiq o'zgarishi faqat stock_movements immutable jurnal orqali, to'g'ridan-to'g'ri UPDATE yo'q" — bu qat'iy loyiha printsipi). Buning o'rniga har qator "maqsad (target) qoldiq" sifatida talqin qilinadi: joriy qoldiq bilan farq (`delta`) hisoblanadi, `delta !== 0` bo'lsa darhol `confirmed` tuzatish hujjati (kirim/spisaniye, xuddi keyingi Task 6 — inventarizatsiya tasdiqlashda rejalashtirilgan pattern bilan bir xil) yaratiladi. Qiziq xossasi: `imported = current + delta` bo'lgani uchun `imported >= 0` talab qilinsa, natija hech qachon manfiy bo'la olmaydi — oldindan `InsufficientStockError` tekshiruvi shart emas.
  Testlash: `exceljs` mock qilinmadi — haqiqiy kutubxona bilan xotirada fayl generatsiya/o'qish (Faza 3'dagi invariant test uslubi), shu jumladan **1000 qatorli mahsulot import** haqiqiy `exceljs` workbook + real `importProductRow` bilan sinaldi (worker'ning parslash mantig'i takrorlanib, natija ~500ms'da tugadi) — Faza 4 Natija mezonining Excel qismini honestly tasdiqlaydi (haqiqiy Postgres/Redis bilan yakuniy tasdiqlash hali qolgan, boshqa fazalardagi bilan bir xil cheklov). 447/447 backend test yashil (27 yangi), lint/format toza.
- Task 5 (inventarizatsiya boshlash + sanoq) bajarildi: `inventory-counts` moduli — `POST /` sklad qoldig'ini shu payt suratga oladi (mavjud `StockRepository.list()` — Faza 3 Task 4 — qayta ishlatildi: har `Stock` qatori uchun `InventoryCountItem` yaratiladi, `systemQty = quantity`, `countedQty` hali `null`), `PATCH /:id/items/:itemId` — hodim `countedQty` kiritganda `diff = countedQty - systemQty` shu yerda hisoblanadi (schema.prisma izohi: "diff — app/service qatlamida hisoblanadi", DB'da generated column emas). Faqat `status:"in_progress"` inventarizatsiyada sanoq kiritish mumkin (`ConflictError` boshqa holatda) — tasdiqlash (`approve`, `status→approved` + avtomatik tuzatish hujjatlari) keyingi vazifada. 463/463 backend test yashil (16 yangi), lint/format toza. Faza 4 — 5/8.
- Task 6 (inventarizatsiya tasdiqlash) bajarildi: `InventoryCountsService.approve()` — barcha qatorda `countedQty` kiritilganini talab qiladi (`ValidationError` aks holda), har `diff !== 0` qator uchun darhol `confirmed` tuzatish hujjati yaratadi. **Refaktoring**: bu mantiq Faza 4 Task 4'dagi (Excel qoldiq import) `importStockRow`dagi bilan deyarli bir xil ekani aniqlandi — endi ikkinchi haqiqiy chaqiruvchi paydo bo'lgani uchun (spekulyativ emas) umumiy `apps/api/src/modules/warehouse-docs/create-confirmed-adjustment.js` (`applyStockAdjustment()`) ga chiqarildi va `import-processors.js`dagi `importStockRow` ham shunga o'tkazildi (xatti-harakati o'zgarmadi, testlar o'zgarishsiz o'tdi). 474/474 backend test yashil (23 yangi), lint/format toza. Faza 4 — 6/8 (qoldi: ikkala frontend vazifa).
- Task 7 (frontend: shtrix-kod/yorliq/Excel) bajarildi. `client.js`'ga `raw: true` konfiguratsiyasi qo'shildi (`.xlsx` binary yuklab olish uchun — mavjud JSON-parslash oqimini buzmasdan). Yangi API modullar: `exports.api.js` (`downloadExport` — Blob orqali brauzer yuklab-olish oynasini ochadi), `imports.api.js` (`uploadImport`/`getImportStatus`), `products.api.js`ga `getProductByBarcode`/`listBarcodes`/`addBarcode`/`removeBarcode` qo'shildi. `BarcodeScanPage.vue` — USB skaner uchun doim fokusdagi matn input (Enter'da qidiradi), kamera uchun brauzer nativ `BarcodeDetector` API (qo'shimcha kutubxonasiz, qo'llab-quvvatlamasa graceful degradation xabari). `ProductFormPage.vue`ga yangi "Shtrix-kodlar" bo'limi (ro'yxat/qo'shish/o'chirish + "Yorliq chop etish" — yangi `lib/print-barcode.js`, `jsbarcode` kutubxonasi bilan canvas'ga chizib yangi oynada `window.print()`). `ProductListPage.vue`ga Excel eksport/import tugmalari (import — fayl tanlash → yuklash → BullMQ job holatini 1.5s intervalda pollash → natija/xato ko'rsatish). `eslint.config.js`ga yangi brauzer global'lar qo'shildi (`URL`/`navigator`/`requestAnimationFrame`/`cancelAnimationFrame`/`BarcodeDetector`).
  Claude Browser'da tekshirildi: skaner sahifasi render bo'ldi, qo'lda kiritish to'g'ri endpoint'ga so'rov yubordi (`/products/by-barcode/:barcode`), "Shtrix-kodlar" bo'limi soxta ma'lumot bilan to'g'ri render bo'ldi. **Shu tekshiruv paytida real bag topildi**: `onExport()`ning xato holatida `importError` o'rnatilardi, lekin xato xabari faqat `importState === "error"` shartida ko'rsatilardi — `onExport` `importState`ni hech qachon o'zgartirmagani uchun xato **jim yutilardi** (UI'da hech narsa ko'rinmasdi). Bu ham tuzatildi (`onExport`ning catch blokida endi `importState.value = "error"` ham o'rnatiladi). `pnpm build` muvaffaqiyatli. 474/474 backend test yashil, lint/format toza.
- Task 8 (frontend: inventarizatsiya) bajarildi — Faza 4ning oxirgi vazifasi. `inventory-counts.api.js` (list/create/get/submitCount/approve). Uch sahifa: `InventoryCountListPage.vue` (sklad/holat filtri, `WarehouseDocListPage.vue` bilan bir xil uslub), `InventoryCountNewPage.vue` (sklad tanlash — bitta maydonli oddiy forma, alohida sahifa sifatida, chunki inventarizatsiya PO/mahsulotdan farqli "yaratish→tahrirlash" emas, butunlay boshqa hayot davri: boshlash→sanoq→tasdiqlash), `InventoryCountDetailPage.vue` (qatorlar jadvali — har qatorda mahalliy kiritma `reactive` obyektda, `@change`da avtomatik saqlanadi — ko'p qatorli inventarizatsiyada har birida alohida "Saqlash" tugmasi noqulay bo'lgani uchun; "Farq" ustuni ikki xil manba — serverga hali yuborilmagan localdagi qiymatdan jonli hisoblanadi (`livePreviewDiff`), aks holda serverdan qaytgan `item.diff`; "Tasdiqlash" tugmasi barcha qatorda `countedQty` kiritilmaguncha o'chirilgan — backend validatsiyasi bilan bir xil qoida, frontendda oldindan ko'rsatiladi).
  Claude Browser'da tekshirildi: ro'yxat/yaratish/tafsilot sahifalari render bo'ldi, client-side validatsiya ("Sklad tanlanishi shart"), soxta ma'lumot bilan tafsilot sahifasida sklad/mahsulot nomlari to'g'ri hal qilindi, mavjud counted qiymat oldindan to'ldirildi, yangi qiymat kiritilganda jonli farq hisoblandi va saqlash so'rovi to'g'ri endpoint'ga yuborildi (backend yo'qligi sababli kutilgan xato UI'da to'g'ri ko'rsatildi). `pnpm build` muvaffaqiyatli. 474/474 backend test yashil (yangi test yo'q — sof frontend vazifa), lint/format toza.
- **Faza 4 — 8/8 vazifa bajarildi.** Counterparties CRUD, shtrix-kod qidiruv+skaner+yorliq, Excel export/import (worker), inventarizatsiya (boshlash→sanoq→tasdiqlash), barcha frontend UI — yozilgan va testlangan. Haqiqiy Postgres/Redis/MinIO/kamera bilan hali tasdiqlanmagan (oldingi fazalardagi bilan bir xil cheklov) — CHECKLIST.md'da ochiq izoh sifatida qoladi.

## Faza 5 — B2B zakaz portali (killer feature)

- Faza 5 boshlandi: plan-rejimda kelishildi (loyihaning asosiy farqlovchi g'oyasi — bir necha arxitektura qarori oldindan kerak edi). TASKS.md 9 vazifaga bo'lindi. `SalePoint`/`Order`/`OrderItem`/`OrderStatusHistory`/`UserAssignment` Faza 0'dan tayyor, `orders.view`/`orders.confirm`/`shop_operator` Faza 1 seed'ida yozilgan-u ishlatilmagan edi. Arxitektura qarorlari: status oqimi MVP kesimi (`new→confirmed→picking→shipped`+`cancelled`, `delivered`/`accepted` Faza 7-8'ga); rezerv — faqat `stock.reserved` (yangi `applyReservedDelta`); jo'natish — bitta darhol-`confirmed` `warehouse_docs` (`issue`, PO/inventarizatsiya'dagi repository-DI pattern); do'kon↔nuqta bog'lanishi — mavjud `UserAssignment` minimal ishlatiladi (to'liq Faza 6 hodimlar UI kutilmaydi); narxlash — `product_prices` orqali, individual chegirma darajalari scope-kesildi; idempotency — check-then-create bitta tranzaksiyada; `apps/shop` — `apps/web` bilan bir xil stack + `vite-plugin-pwa` (birinchi marta).
- Task 1 (sale points backend) bajarildi: `sale-points` moduli (`warehouses`/`counterparties` qolipi). Muhim qaror — DATABASE.md'ga ko'ra har nuqtaga **avtomatik** `Counterparty` (type:customer) ochiladi (foydalanuvchi alohida kontragent tanlamaydi) — `SalePointsService.create()` bitta tranzaksiyada avval `Counterparty`, keyin shunga bog'langan `SalePoint` yaratadi; `update()`da `creditLimit`/`paymentTermDays`/`phone` berilsa bog'liq kontragent ham yangilanadi. Yangi kichik `user-assignments` moduli (faqat repository, o'z routes'i yo'q — `sale-points`ga DI orqali ulanadi, PO/inventarizatsiya'dagi repository-darajasidagi kompozitsiya qolipi bilan bir xil) — operator biriktirish/olib tashlash/ro'yxat, `findSalePointIdForUser()` (Task 7-8'da `apps/shop` uchun kerak bo'ladi). `seed.js`: yangi `sale_points.manage` ruxsat kodi + mavjud-u ishlatilmagan `orders.view`/`orders.confirm` `warehouse_manager`/`picker` rollariga birinchi marta biriktirildi (yangi `ROLE_PERMISSIONS` xaritasi — oldin faqat owner'ga hammasi berilardi). Yon ish: `pnpm lint`/`format:check` fon vazifasi (`task_ad5d6e3b`, alohida `.claude/worktrees/` git worktree'da ishlayotgan) natijasida ifloslanayotgani aniqlandi — `eslint.config.js`/`.prettierignore`ga `.claude/worktrees/` (va `.impeccable/`) qo'shildi, bu boshqa sessiyaning ishiga tegmaydi, faqat asosiy tree'ning lint/format natijasini tozalaydi. 505/505 backend test yashil (31 yangi), lint 0 xato/format toza. Faza 5 — 1/9.
- Task 2 (orders backend: yaratish) bajarildi: `#computeQtyBase` `warehouse-docs.service.js`dan (va xuddi shu nusxa `purchase-orders.service.js`dan — uchinchi haqiqiy chaqiruvchi paydo bo'lgani sabab shuni ham) umumiy `apps/api/src/lib/qty-base.js`ga chiqarildi, uchala modul ham shundan foydalanadi (xatti-harakat o'zgarmadi, mavjud testlar o'zgarishsiz o'tdi). Yangi `orders` moduli — `POST/GET /api/v1/orders`, `GET /api/v1/orders/:id`. Muhim qarorlar: (1) `salePointId` client tomonidan yuborilmaydi — `UserAssignmentsRepository.findSalePointIdForUser()` orqali auth'dan hal qilinadi (topilmasa `ForbiddenError`); (2) narx `ProductPricesRepository.listCurrentByProduct()`dan sotuv nuqtasining `priceTypeId`si bo'yicha snapshot qilinadi (topilmasa `NotFoundError`) — variant-maxsus narxlash Faza 2'dagi mavjud `distinct:["priceTypeId"]` cheklovi sababli MVP'da priceType darajasida qoladi (PLAN.md'ning "variantlar" scope-kesish ro'yxatiga mos); (3) idempotency — `findByIdempotencyKey()` bitta tranzaksiyada avval tekshiriladi, topilsa mavjud zakaz qaytariladi (replay), UNIQUE(companyId, idempotencyKey) DB darajasida oxirgi himoya; (4) `list()`/`getById()` — `orders.view` ruxsati bo'lsa (sklad tomoni) butun kompaniya, bo'lmasa (do'kon operatori) faqat o'z sotuv nuqtasi zakazlari (`RolesRepository.hasPermission` shu servisga birinchi marta to'g'ridan-to'g'ri DI qilindi, avval faqat middleware'da ishlatilardi). Zakaz raqami `ZAK-YYYY-NNNNN` (`doc_counters`, `docType:"order"` — warehouse-docs/purchase-orders bilan bir xil atomik naqsh). 532/532 backend test yashil (27 yangi: orders 24 + qty-base 3), lint 0 xato/format toza. Faza 5 — 2/9.
- Task 3 (orders backend: tasdiqlash/bekor qilish) bajarildi: `StockRepository`ga yangi `applyReservedDelta()` (`applyDelta()`ning rezerv varianti — faqat `stock.reserved`ni upsert+increment bilan o'zgartiradi, `quantity` tegilmaydi). `OrdersService.confirm()` — faqat `status:"new"`dan (`ConflictError` boshqa holatda), har item uchun `(quantity - reserved) >= qtyBaseOrdered` tekshiriladi (yetarli bo'lmasa `InsufficientStockError`, butun tasdiqlash bekor — qisman rezerv yo'q, `warehouse-docs`dagi bilan bir xil "check-then-apply, tranzaksiya rollback atomiklikni kafolatlaydi" naqsh), qulflar productId/variantId bo'yicha leksikografik tartibda. `dueDate` shu yerda hisoblanadi (`paymentTermDays` — zakaz yaratishda counterparty'dan snapshot qilingan qiymat, Task 2). `cancel()` — faqat `new`/`confirmed`/`picking`dan (`shipped`dan keyin taqiqlangan, storno emas — qaytarish/vozvrat Faza 7 ishi), rezerv faqat `confirmed`/`picking` holatida bo'lgani uchun faqat o'sha holatlarda bo'shatiladi (`new`da hali rezerv yo'q — bo'shatish shart emas). Ikkalasi ham `order_status_history`ga yozadi. Marshrutlar `orders.confirm` ruxsati bilan (Task 1'da `warehouse_manager`/`picker`ga biriktirilgan). 546/546 backend test yashil (14 yangi), lint 0 xato/format toza. Faza 5 — 3/9.
- Task 4 (orders backend: yig'ish/jo'natish) bajarildi: `pick()` — `confirmed→picking`, stock'ga tegmaydi (sof sklad navbati belgisi, pick list UI shu status bilan filtrlanadi). `ship()` — `picking→shipped`: bitta darhol-`confirmed` `warehouse_docs` (`type:"issue"`, `counterpartyId` — sotuv nuqtasining kontragenti, `number: CHIQ-YYYY-NNNNN`) yaratadi, `WarehouseDocsRepository`/`StockRepository`/`StockMovementsRepository`ga to'g'ridan-to'g'ri DI orqali bog'lanib bitta `withTenant` tranzaksiyasida: har item uchun hujjat qatori qo'shadi, `stock.quantity` VA `stock.reserved`ni birga kamaytiradi (jo'natilgan miqdor avval `confirm()`da rezerv qilingan edi), immutable `stock_movements` yozadi, `OrderItem.qtyShipped`/`qtyBaseShipped`ni yangilaydi. Qisman jo'natish: `dto.items` orqali har item uchun ixtiyoriy kam miqdor berilishi mumkin (`qtyBaseShip` proporsional hisoblanadi — `qtyBaseOrdered * shipQty/qtyOrdered`, buyurtmadan ko'p bo'lsa `ValidationError`); jo'natilmagan qoldiq (`qtyOrdered - qtyShipped`) **backorder sifatida avtomatik ishlanmaydi** — PLAN.md 6.3'dagi "sozlanadi" qoidasiga ko'ra ataylab MVP scope-kesish. Ikkalasi ham `orders.confirm` ruxsati bilan (mavjud sklad-tomoni ruxsat kodi qayta ishlatildi, yangi kod qo'shilmadi). 553/553 backend test yashil (20 yangi), lint 0 xato/format toza. **Faza 5 backend — 4/9 (orders moduli to'liq: yaratish→tasdiqlash→yig'ish→jo'natish→bekor qilish). Qoldi: shop-catalog backend, apps/shop (3 vazifa), apps/web sklad UI.**
- Task 5 (shop-catalog backend) bajarildi: yangi `shop-catalog` moduli — `GET /api/v1/shop-catalog?search=&warehouseId=`. `exports` modulidagi bilan bir xil "repository-only aggregatsiya" qolipi (o'z jadvali yo'q — `.repository.js` yozilmadi, mavjud `products`/`product-prices`/`sale-points`/`user-assignments`/`stock` repositorylari DI orqali birlashtiriladi). Do'kon operatorining sotuv nuqtasi `UserAssignmentsRepository.findSalePointIdForUser()` orqali auth'dan hal qilinadi (orders bilan bir xil), har mahsulot uchun narx sotuv nuqtasining `priceTypeId`si bo'yicha qidiriladi — **narx belgilanmagan yoki arxivlangan mahsulot katalogda umuman ko'rinmaydi** (jim filtrlanadi, xato emas — bu qasddan: do'kon operatori narxi yo'q narxni ko'rmasligi kerak). `warehouseId` berilsa `availableQty = quantity - reserved` qo'shiladi (berilmasa `null` — frontend narxni ko'rsatadi, qoldiqni ko'rsatmaydi). 561/561 backend test yashil (15 yangi), lint 0 xato/format toza. Faza 5 backend — 5/9. Qoldi: `apps/shop` (3 vazifa) va `apps/web` sklad UI.
- Task 6 (`apps/shop` skeleti) bajarildi — birinchi marta yangi frontend ilova qo'shildi (Faza 0'da yaratilgan bo'sh placeholder to'ldirildi). Xuddi shu stack/qolip `apps/web`dan nusxa: Vue 3.5/Vite/Tailwind4/Pinia/TanStack Query/vue-router, xuddi shu `auth.store.js`/`client.js`/`Login`/`SelectCompany` sahifalari, `components/ui` (Button/Input/Label/Card) — `packages/ui`ga ko'chirish ataylab qilinmadi (hali bo'sh placeholder, BACKLOG'ga qoldirildi, scope-creep oldini olish uchun). Yangi bog'liqlik `vite-plugin-pwa` — loyihada birinchi marta ishlatildi (manifest + avtomatik service worker, mavjud `favicon.svg` PWA ikonkasi sifatida qayta ishlatildi — PNG generatsiya qilinmadi, keyinroq sayqallash mumkin). Dev port 5174 (`apps/web` — 5173dan farqli), `.claude/launch.json`ga `shop` konfiguratsiyasi qo'shildi (mavjud `web`/`api` saqlanib qoldi — birinchi urinishda ularni bilmasdan bosib yuborib, `git checkout`bilan tiklandi). `ShopLayout.vue` — mobile-first: header (logo+kompaniya+chiqish) + pastki navigatsiya (`nav` fixed bottom, PLAN.md "pastki navigatsiya" talabi) — hozircha faqat "Katalog" havolasi, Task 7-8 to'ldiradi. Router: `/login`, `/select-company`, `/` (layout) → `home` (vaqtinchalik placeholder sahifa, Task 7 haqiqiy katalogga almashtiradi). `pnpm build` muvaffaqiyatli (PWA service worker generatsiya qilindi), lint/format toza. Claude Browser'da tekshirildi: login sahifasi render bo'ldi, client-side Zod validatsiya ishladi, to'liq login oqimi (fetch monkey-patch bilan) muvaffaqiyatli — `ShopLayout` header/bottom-nav to'g'ri render bo'ldi, mobile viewport (375px)da DOM o'lchamlari orqali full-width tasdiqlandi (skrinshot asboб o'zining device-pixel-ratio artifacti bor edi, bu kod xatosi emas). Faza 5 — 6/9.
- Task 7 (`apps/shop`: katalog/savat/checkout) bajarildi: `shop-catalog.api.js`/`warehouses.api.js`/`orders.api.js` yangi API klientlar. Yangi `cart.store.js` (Pinia) — lokal savat, "bitta savat = bitta sklad" qarori (sklad almashtirilsa savat tozalanadi — narx sotuv nuqtaga bog'liq, qoldiq skladga xos, ikkalasi aralashmasligi uchun). `CatalogPage.vue` (avvalgi Task 6 placeholder `HomePage.vue`ning o'rnini bosdi — sklad tanlash dropdown + qidiruv debounce, `apps/web`dagi `ProductListPage.vue` uslubi) va `CartPage.vue` (miqdor tahrirlash, `Buyurtma berish` → `POST /orders`, muvaffaqiyatda tasdiqlash ekrani + savat tozalanadi). `ShopLayout.vue`ga "Savat (N)" bo'limi qo'shildi.
  **Claude Browser'da to'liq tekshiruv real bag topdi va tuzatdi**: `Input`/`Button` (`components/ui`) `$attrs.class`ni `cn()` orqali birlashtirmagan edi (`Card.vue`da bor edi, ikkalasida yo'q) — Vue'ning avtomatik class-inheritance'i konfliktli Tailwind klasslarini (`class="w-16"` vs komponentning ichki `w-full`i) to'g'ri hal qila olmadi, natijada `CatalogPage`da miqdor input'i butun qatorni egallab, mahsulot nomi/narxi ko'rinmas bo'lib qoldi. Bu **`apps/web`da ham xuddi shu, oldindan mavjud, sezilmagan bag** ekani aniqlandi (`ProductFormPage.vue`/`WarehouseDocFormPage.vue`dagi `<Input class="w-28"/>="w-24"`lar ham xuddi shu sababdan haqiqiy tor emas, to'liq kenglikda render bo'lib turgan) — ikkala ilovada ham (`apps/web` va `apps/shop`) `Input.vue`/`Button.vue` tuzatildi (`cn(..., $attrs.class)`, `Card.vue`dagi bilan bir xil qolip). Checkout oqimi (savat→buyurtma→tasdiqlash ekrani) to'liq soxta `fetch` bilan tekshirildi. `pnpm build` muvaffaqiyatli, backend 561/561 test yashil (o'zgarish yo'q — sof frontend), lint 0 xato/format toza. Faza 5 — 7/9.
- Task 8 (`apps/shop`: zakazlarim) bajarildi — `apps/shop`ning oxirgi vazifasi. `orders.api.js`ga `listOrders()`/`getOrder(id)` qo'shildi (backend `OrdersService.list()`ning `orders.view` ruxsatisiz chaqiruvchini avtomatik o'z sotuv nuqtasiga cheklashi — Task 2'dan — shu yerda qayta ishlatiladi, frontend filtr yubormaydi). `OrdersListPage.vue` (raqam/status/summasi, status→o'zbekcha label xaritasi) va `OrderDetailPage.vue` (qatorlar, jami). Kichik texnik nuqta: `OrderItem` faqat `productId` saqlaydi (`warehouse_docs`dagi bilan bir xil konvensiya, nom saqlanmaydi) — mahsulot nomini ko'rsatish uchun mavjud `GET /shop-catalog`dan (do'kon operatorining yagona ko'ra oladigan mahsulot ro'yxati) `productId→nameUz` xaritasi tuziladi, yangi backend endpoint kerak bo'lmadi. `ShopLayout.vue`ga "Zakazlarim" bo'limi, `CartPage.vue`ning tasdiqlash ekraniga "Zakazni ko'rish" havolasi qo'shildi. Claude Browser'da to'liq tekshirildi: ro'yxat/tafsilot to'g'ri render bo'ldi, mahsulot nomlari to'g'ri hal qilindi. `pnpm build` muvaffaqiyatli, lint 0 xato/format toza. **`apps/shop` — 3/3 vazifa tugadi (skelet→katalog/savat/checkout→zakazlarim).** Faza 5 — 8/9, qoldi faqat `apps/web` sklad tomoni UI (Task 9, fazaning oxirgi vazifasi).
- Task 9 (`apps/web`: sklad tomoni UI) bajarildi — Faza 5ning oxirgi vazifasi. **Arxitektura qarori shu vazifada aniqlandi**: "operator biriktirish" UI uchun kompaniya a'zolarini ro'yxatlab tanlash endpoint'i hali yo'q edi (to'liq hodimlar boshqaruvi Faza 6). Yangi userId-listing endpoint qo'shish o'rniga — `assignOperator` DTO `userId`dan **`phone`**ga o'zgartirildi: mavjud (ishlatilmagan) `UsersRepository.findByPhone()` orqali server tomonda hal qilinadi (operator o'zi shu raqam bilan login qiladi, eng tabiiy identifikator). `packages/shared`/`sale-points.service.js`/`.controller.js`/`.routes.js` va ularning testlari shunga moslab yangilandi (yangi backend test yo'q, mavjudlari `userId` o'rniga `phone` bilan qayta yozildi — xatti-harakat kengaydi, buzilmadi).
  Yangi frontend: `sale-points.api.js`/`orders.api.js` (`apps/web`), `SalePointListPage.vue`/`SalePointFormPage.vue` (CRUD + operator biriktirish/olib tashlash — `ProductFormPage.vue`dagi bilan bir xil create/edit qolipi), `OrderListPage.vue` (holat filtri, `WarehouseDocListPage.vue` uslubi), `OrderDetailPage.vue` (**pick list ko'rinishi** — holatga qarab tugmalar: `new`→Tasdiqlash, `confirmed`→Yig'ishni boshlash, `picking`→har item uchun miqdor tahrirlanadigan Jo'natish, `new/confirmed/picking`→Bekor qilish). `AppLayout.vue`ga "Sotuv nuqtalari"/"Zakazlar" bo'limlari.
  Claude Browser'da **to'liq E2E oqim ikkala ilovada birga tekshirildi**: `apps/shop`da soxta login+katalog+savat+checkout, so'ng `apps/web`da sklad tomonidan xuddi shu zakazni tasdiqlash→yig'ishni boshlash→jo'natish — holat ekranda to'g'ri `Yangi→Tasdiqlangan→Yig'ilmoqda→Yo'lda` bo'lib o'zgardi, pick list'dagi "Jo'natiladi" ustuni tahrirlanadigan input sifatida to'g'ri render bo'ldi va jo'natilgandan keyin "Jo'natilgan" ustuniga to'g'ri qiymat (2) yozildi. Operator biriktirish UI ham (telefon bo'yicha) tekshirildi. Ikkala frontend `pnpm build` muvaffaqiyatli, backend 563/563 test yashil (o'zgarish yo'q — sof frontend + kichik service refaktor), lint 0 xato/format toza.
  **Faza 5 — 9/9 vazifa bajarildi.** Sotuv nuqtalari boshqaruvi, `apps/shop` (skelet/katalog/savat/checkout/zakazlarim), `orders` moduli (yaratish→tasdiqlash→yig'ish→jo'natish→bekor qilish), `apps/web` sklad tomoni UI — barchasi yozilgan, testlangan, Claude Browser'da E2E tekshirilgan. Haqiqiy Postgres/Redis/PWA o'rnatish (real telefon) bilan hali tasdiqlanmagan (oldingi fazalardagi bilan bir xil infratuzilma cheklovi) — CHECKLIST.md'da ochiq izoh sifatida qoladi.

## Faza 6 — Hodimlar va bildirishnomalar

- Faza 6 boshlandi: plan-rejimda kelishildi (besh deyarli mustaqil quyi-tizim — hodimlar/rollar, SMS, parol tiklash, hodisalar+bildirishnomalar+Socket.IO, Web Push — boshqa fazalardan ko'proq arxitektura qarori talab qildi). TASKS.md 8 vazifaga bo'lindi. Arxitektura qarorlari: hodim yaratish **parolsiz** + bir martalik taklif/parol-tiklash tokeni (Redis, `pwreset:{token}`) uchta joyda qayta ishlatiladi (taklif/admin-reset/o'z-o'zini tiklash); SMS (Eskiz.uz) va Web Push (VAPID) — ikkalasi ham env yo'q bo'lsa graceful no-op+log (Sentry DSN'siz ishlash bilan bir xil konvensiya); domen hodisalari — Node `EventEmitter` (`order.new`), `notifications` moduli obuna bo'ladi; Socket.IO — JWT auth, company-xona; **`PushSubscription`** — Faza 0'dan beri birinchi haqiqiy yangi Prisma jadval (DATABASE.md'da yo'q edi, hujjat bo'shlig'i, PLAN.md asosiy MVP ro'yxatida Web Push aniq bor).
- Task 1 (hodimlar+rollar backend) bajarildi: `company-members.repository.js`ga (avval faqat `create`/`findByUserId` bor edi) `findByCompanyAndUser`/`findById`/`list`/`update` qo'shildi, yangi `company-members.service.js`/`.controller.js`/`.routes.js` (`employees.manage` ruxsati). Hodim yaratish — **parolsiz**: `passwordHash`ga hech kimga ma'lum bo'lmagan tasodifiy qiymat yoziladi (Django "unusable password" konsepsiyasiga o'xshash, NOT NULL ustunni buzmasdan "parol hali yo'q" holatini ifodalaydi — haqiqiy parol o'rnatish Task 2'da). Telefon bo'yicha mavjud `User` bo'lsa yangisi yaratilmaydi (bitta odam bir nechta kompaniyada ishlashi mumkin, PLAN.md) — faqat yangi `CompanyMember` qo'shiladi. Ixtiyoriy `assignments` (sklad/nuqta) — mavjud `UserAssignmentsRepository` DI orqali (Faza 5'dan). Bloklashda hodimning **barcha aktiv sessiyalari** darhol bekor qilinadi (mavjud `SessionsRepository.listByUser`+`revoke()`, yangi metod kerak bo'lmadi).
  `roles.repository.js`ga (avval faqat `findSystemRoleByName`/`hasPermission`) `create`/`findById`/`list`/`update`/`listAllPermissions`/`listPermissionIdsForRole`/`setRolePermissions` qo'shildi, yangi `roles.service.js`/`.controller.js`/`.routes.js`. Tizim rollari (`isSystem:true`) o'zgartirilmaydi (`ForbiddenError`) — faqat kompaniyaning o'z maxsus rollari. Ruxsatlar matritsasi: `GET /roles/permissions` (barcha `Permission`), `GET/PUT /roles/:id/permissions` (rolga biriktirilgan/almashtirilgan). 609/609 backend test yashil (46 yangi), lint 0 xato/format toza. Faza 6 — 1/8.
- Task 2 (SMS + taklif/parol-tiklash tokenlari) bajarildi: yangi `lib/sms.js` — Eskiz.uz token-based auth (`Redis`da `eskiz:token` ~29 kun keshlanadi, muddati o'tsa avtomatik qayta login), `sendSms(phone, message)` — `ESKIZ_EMAIL`/`ESKIZ_PASSWORD` yo'q bo'lsa haqiqiy so'rov yubormaydi, faqat `logger.warn` (Sentry DSN'siz ishlash bilan bir xil konvensiya), xatolikda ham otilmaydi (best-effort). Yangi `password-reset.repository.js` (Redis, `pwreset:{token}` → `userId`, 24 soat TTL, bir martalik — `consumeToken()` o'qib darhol o'chiradi). `UsersRepository`ga `update()` qo'shildi (avval yo'q edi). `AuthService.setPassword()` — token orqali parol o'rnatadi, muvaffaqiyatdan keyin foydalanuvchining BARCHA sessiyalari bekor qilinadi; `POST /auth/set-password` (validatsiyasiz, ochiq endpoint — token o'zi autentifikatsiya vazifasini bajaradi).
  `CompanyMembersService.create()` endi tranzaksiyadan **tashqarida** (tarmoq I/O qoidasi, Faza 2/4'dagi bilan bir xil) — faqat yangi (parolsiz) foydalanuvchiga taklif tokeni yaratib SMS yuboradi (mavjud foydalanuvchi allaqachon parolga ega, SMS shart emas). Yangi `resetPassword(auth, id)` — ega tomonidan majburiy tiklash: eski parol darhol "unusable" qiymatga almashtiriladi (hodim yaratishdagi bilan bir xil naqsh — parol darhol ishlamay qoladi, faqat sessiyalarni bekor qilish yetarli emas edi), sessiyalar bekor qilinadi, yangi taklif tokeni SMS bilan yuboriladi (`POST /company-members/:id/reset-password`). Kichik texnik nuqta: eslint'ga Node native `fetch`/`URLSearchParams` global'lari qo'shildi (`lib/sms.js` ishlatgani uchun, avval faqat brauzer blokida bor edi). 627/627 backend test yashil (18 yangi, jumladan `vi.resetModules()`+`vi.doMock()` bilan turli `ESKIZ_*` env holatlarini sinaydigan `sms.test.js`), lint 0 xato/format toza. Faza 6 — 2/8.
- Task 3 (o'z-o'zini parolni tiklash) bajarildi: yangi `otp.repository.js` — 6 xonali kod Redis **hash**da (`otp:{phone}` — `code`+`attempts`, 3 daqiqa TTL) saqlanadi, oddiy JSON-string emas (`hincrby` bilan urinishlar sonini TTL'ni buzmasdan atomik oshirish uchun tanlangan qaror). `AuthService.forgotPassword()` — **foydalanuvchi mavjudligini oshkor qilmaydi** (user enumeration himoyasi): telefon ro'yxatdan o'tmagan bo'lsa ham har doim bir xil 204 javob, faqat SMS jim yuborilmaydi. `resetPasswordWithOtp()` — kod/urinish limiti (`MAX_OTP_ATTEMPTS=3`) tekshiriladi, muvaffaqiyatda parol yangilanadi + barcha sessiyalar bekor qilinadi (Task 2'dagi `setPassword()` bilan bir xil yakuniy naqsh). Yangi marshrutlar: `POST /auth/forgot-password` (IP bo'yicha alohida rate-limit — `rl:forgot-password`, 15 daqiqada 5 ta — SMS-spam oldini olish uchun, mavjud `rateLimit()` middleware'i qayta ishlatildi) va `POST /auth/reset-password`. `.env.example`ga `ESKIZ_*`/`APP_WEB_URL`/`APP_SHOP_URL` hujjatlashtirildi. 645/645 backend test yashil (18 yangi), lint 0 xato/format toza. **Auth moduli parol-tiklash uch oqimi ham (taklif/admin-majburiy/o'z-o'zini) to'liq.** Faza 6 — 3/8, qoldi: domen hodisalari+bildirishnomalar, Socket.IO, Web Push, ikkita frontend vazifa.
- Task 4 (domen hodisalari + bildirishnomalar) bajarildi: yangi `lib/events.js` — Node `EventEmitter`ning bitta umumiy nusxasi (`domainEvents`), CLAUDE.md'dagi "modullar EventEmitter orqali bog'lanadi" printsipi birinchi marta shu yerda amalga oshdi. `OrdersService.create()` — muvaffaqiyatli (idempotent replay emas) yaratilgandan keyin, tranzaksiyadan **tashqarida**, `domainEvents.emit("order.new", {...})` chaqiradi. Yangi `notifications` moduli (repository/service/controller/routes — `Notification` jadvali Faza 0'dan tayyor edi) — `notifications.routes.js` o'z kompozitsiya vaqtida `domainEvents.on("order.new", ...)` ro'yxatdan o'tkazadi (`orders` moduli bu servisni bilmaydi, haqiqiy loose coupling). `notifyOrderNew()` — kompaniyaning aktiv a'zolaridan faqat `orders.confirm` ruxsatiga egalarga (MVP filtri, "faqat shu skladga biriktirilganlar" nozikroq qoidasi BACKLOG'da) `Notification` qatori yaratadi. 670/670 backend test yashil (25 yangi), lint 0 xato/format toza. Faza 6 — 4/8.
- Task 5 (Socket.IO) bajarildi: yangi `lib/socket.js` — `initSocket(httpServer)` Socket.IO server yaratadi, `io.use()` middleware handshake'dagi JWT access token'ni (`requireAuth` bilan bir xil `verifyToken()`) tekshiradi, muvaffaqiyatli bo'lsa socket `company:{companyId}` xonasiga qo'shiladi. `index.js`da `app.listen()`ning qaytgan `http.Server`i endi o'zgaruvchiga saqlanib `initSocket()`ga beriladi (avval e'tiborsiz tashlanardi). `emitToCompany(companyId, event, payload)` — best-effort (`initSocket()` chaqirilmagan bo'lsa, masalan testlarda, jim o'tkazib yuboradi). `NotificationsService.notifyOrderNew()` endi har yaratilgan bildirishnoma uchun (tranzaksiyadan tashqarida) `emitToCompany(companyId, "notification", notification)` ham chaqiradi. `socket.io` yangi bog'liqlik — native qism yo'q. Testlash: `socket.io`ning `Server` klassi mock qilinib, `io.use`/`io.on` handler'lari qo'lda chaqirilib auth middleware+xona-qo'shish mantig'i tekshirildi. 688/688 backend test yashil (18 yangi), lint 0 xato/format toza. Faza 6 — 5/8.
- Task 6 (Web Push backend) bajarildi: **Faza 0'dan beri birinchi haqiqiy yangi Prisma jadval** — `PushSubscription` (`id, userId, endpoint UNIQUE, p256dh, auth, createdAt`, RLS'siz — `company_id` yo'q, foydalanuvchiga bog'liq, izolyatsiya servis qatlamida `userId` bo'yicha), DATABASE.md'dagi hujjat bo'shlig'i shu bilan yopildi. Yangi `lib/web-push.js` — `web-push` npm paketi, `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY` yo'q bo'lsa `sms.js` bilan bir xil graceful no-op+log konvensiyasi; `sendWebPush()` 404/410 xatoni `{expired:true}` deb qaytaradi (chaqiruvchi obunani o'chirishi uchun signal, boshqa xatolar faqat logga yoziladi). Yangi `push-subscriptions` moduli — `POST /` (upsert `endpoint` bo'yicha — brauzer bir xil obunani qayta yuborishi mumkin), `DELETE /:id` (faqat o'z obunasi, `NotFoundError` aks holda). `NotificationsService.notifyOrderNew()` yakuniy ko'rinishga keldi: DB yozuv (tranzaksiya ichida) → Socket.IO emit + har foydalanuvchining barcha push-obunalariga `sendWebPush()` (tranzaksiyadan tashqarida, ikkalasi ham best-effort). 688/688 backend test yashil (barchasi allaqachon sanalgan — bu vazifa asosan yangi fayllar test bilan qo'shdi), lint 0 xato/format toza, `prisma validate`/`generate` o'tdi (haqiqiy migratsiya hali qo'llanilmagan — mahalliy Postgres yo'q, oldingi fazalardagi bilan bir xil cheklov). Faza 6 backend — 6/8 tugadi, qoldi ikkita frontend vazifa.
- Task 7 (frontend: hodimlar/rollar/real-time, `apps/web`) bajarildi: yangi `company-members.api.js`/`roles.api.js`/`push-subscriptions.api.js`. `EmployeeListPage.vue`/`EmployeeFormPage.vue` (yaratish — telefon/F.I.O/rol/sklad-nuqta biriktirish ro'yxati; tahrirlash — rol/holat + "Parolni tiklash" tugmasi), `RoleListPage.vue` (ro'yxat+yangi rol yaratish) / `RolePermissionsPage.vue` (checkbox matritsa, tizim rolida faqat ko'rish). Socket.IO klient: yangi `lib/socket.js` (`connectSocket()` — JWT bilan ulanadi; `playDing()` — Web Audio API orqali qisqa tovush, alohida audio fayl shart emas), `AppLayout.vue`ga ulandi — `authStore.accessToken` mavjud bo'lganda ulanadi, `"notification"` hodisasida pastki-o'ngdagi mahalliy toast ro'yxatiga qo'shadi+tovush chalinadi (alohida toast kutubxonasi ishlatilmadi, oddiy `reactive` ro'yxat yetarli). `PushSubscribeButton.vue` — `Notification.requestPermission()`+`PushManager.subscribe()`+backend'ga yuborish. `vite-plugin-pwa` `apps/web`ga birinchi marta qo'shildi — `strategies:"injectManifest"` (precache emas, faqat maxsus `src/sw.js` — `push`/`notificationclick` handler'lari), monorepo ildizidagi bitta `.env`dan `VITE_VAPID_PUBLIC_KEY`ni o'qish uchun `envDir` sozlandi. Dev serverga `/socket.io` ws-proxy qo'shildi. Claude Browser'da tekshirildi: barcha yangi sahifalar (hodimlar ro'yxati/forma — biriktirish qatori qo'shish ishladi, rollar ro'yxati, ruxsatlar matritsasi) Pinia orqali soxta auth bilan to'g'ri render bo'ldi, nav'da "Hodimlar" havolasi va header'da "🔔 Bildirishnomalar" tugmasi ko'rindi, konsolda JS xatosi yo'q. `pnpm build` (ikkalasi — asosiy bundle + `sw.js`) muvaffaqiyatli, lint 0 xato/format toza. Faza 6 — 7/8.
- Task 8 (frontend: parolni tiklash, `apps/web`+`apps/shop`) bajarildi — Faza 6ning oxirgi vazifasi. Ikkala ilovaga bir xil: `auth.api.js`ga `forgotPassword`/`resetPassword`/`setPassword`, yangi `ForgotPasswordPage.vue` (ikki bosqichli forma — telefon→kod yuborish, keyin kod+yangi parol; bitta sahifa ichida `step` state bilan, alohida marshrut shart emas) va `SetPasswordPage.vue` (URL `?token=`dan o'qiladi, token yo'q bo'lsa "Havola yaroqsiz" xabari, muvaffaqiyatda avtomatik `/login`ga qaytaradi). `LoginPage.vue`ga "Parolni unutdingizmi?" havolasi ikkala ilovada ham qo'shildi. Claude Browser'da tekshirildi: telefon-validatsiya xabari to'g'ri ko'rsatildi, token bor/yo'q ikkala holat `SetPasswordPage`da to'g'ri render bo'ldi, login sahifasida yangi havola ko'rindi, konsolda xato yo'q. Ikkala ilova `pnpm build` muvaffaqiyatli, lint 0 xato/format toza.
- **Faza 6 — 8/8 vazifa bajarildi.** Hodimlar+rollar backend/UI, SMS taklif/parol-tiklash (uch oqim: taklif/admin-majburiy/o'z-o'zini OTP), domen hodisalari+bildirishnomalar, Socket.IO real-time, Web Push (yangi `PushSubscription` jadvali bilan), ikkala frontend ilova (parolni tiklash sahifalari) — barchasi yozilgan, testlangan (688/688 backend test), Claude Browser'da tekshirilgan. "Hodim statistikasi" (CHECKLIST.md'da bor edi) 8 vazifalik rejaga kiritilmagan — BACKLOG.mgga o'tkazildi. Haqiqiy Postgres/Redis/Socket.IO/Web Push (real brauzer push) bilan hali tasdiqlanmagan (oldingi fazalardagi bilan bir xil infratuzilma cheklovi) — CHECKLIST.md'da ochiq izoh sifatida qoladi.
- Faza 7 (dostavka va kuryer xaritada) boshlandi: plan-rejimda kelishildi. Muhim topilma — `Delivery`/`DeliveryOrder`/`CourierLocation` Prisma modellari va `Order.status`ning `shipped`/`delivered`/`accepted` qiymatlari Faza 0/5'dan tayyor edi (yangi migratsiya kerak emas), faqat kod yozilmagan edi — bu faza asosan mavjud sxemani yig'ish. Arxitektura qarorlari: dostavka = allaqachon `shipped` zakazlarni marshrutga yig'ish; kuryer bekatni yetkazganda 4 xonali `acceptCode` generatsiya qiladi (`DeliveryOrder.acceptCode` — Faza 0'dan bor-u ishlatilmagan ustun, PLAN.md F "imzo/kod" talabi shu yerda amalga oshdi), do'kon qabul qilishda shu kodni kiritadi; GPS retention — PLAN.md asli "kunlik BullMQ job" deb yozgan edi, lekin `rls.sql`ning o'zi buni izohlaydi (cross-tenant RLS-bypass roli Faza 11'gacha yo'q) — shuning uchun har GPS yozuvida **shu kompaniya doirasida** eskilarni tozalash (tenant-ichi sweep, global cron o'rniga) qarori qabul qilindi.
- Task 1 (deliveries backend) bajarildi: yangi `deliveries` moduli (repository/service/controller/routes/schemas+testlar, Faza5/6 qolipi bilan bir xil). `POST /deliveries` faqat `status:"shipped"` zakazlarni qabul qiladi, `cashExpected` = tanlangan zakazlar yig'indisi. `POST /:id/orders/:orderId/deliver` — egalik tekshiruvi (`companyMembersRepository.findByCompanyAndUser`, ruxsat emas — push-subscriptions'dagi "faqat o'zinikini" naqshi), barcha bekat yetkazilsa `Delivery.status="done"` avtomatik. Yangi `deliveries.manage` ruxsati (seed: owner+warehouse_manager). 32 yangi test, 720/720 backend yashil.
- Task 2 (qabul qilish + qaytarish, orders moduliga qo'shimcha) bajarildi: `accept()` — `delivered→accepted`, `acceptCode` tekshiradi, har item uchun `qtyAccepted`/`qtyBaseAccepted` yozadi (farqlar akti — kam kelgan qism alohida hujjatsiz, faqat shu ustunlar orqali ko'rinadi, moliyaviy oqibati Faza 8/9'ga bog'liq). `returnItems()` — `accepted`dan keyin, `ship()`ning aynan teskarisi (`receipt` hujjat, `stock.quantity`↑, `stock_movements`↑), order o'zi o'zgarmaydi (immutable — qaytarish mustaqil hujjat). 729/729 backend yashil.
- Task 3 (kuryer GPS — Socket.IO + retention) bajarildi: yangi `courier-locations` moduli (faqat Socket.IO orqali chaqiriladi, REST route yo'q). `lib/socket.js`ga `courier:location` listener — koordinatani yozadi va **shu tranzaksiyada** shu kompaniya doirasida 30 kundan eski `courier_locations` yozuvlarini o'chiradi, so'ng `emitToCompany(..., "courier:position", ...)` (Faza 6'dagi mavjud funksiya qayta ishlatildi). `ordersRepository.findById`ga `deliveryOrders.delivery` include qo'shildi — do'kon "kim yetkazyapti"ni bilishi uchun. 736/736 backend yashil, lint/format toza.
- Task 4-6 (frontend, `apps/web`) bajarildi: dispetcher tomoni — `DeliveryListPage.vue`/`DeliveryFormPage.vue` (shipped zakazlarni checkbox bilan tanlab kuryerga biriktirish)/`DeliveryDetailPage.vue`, `deliveries.api.js`. Jonli xarita — `leaflet` birinchi marta o'rnatildi (Vue wrapper'siz, oddiy JS + `L.divIcon` — bundler'da standart marker ikon yo'li buzilish muammosidan qochish uchun), `DeliveryMapPage.vue` barcha kuryerlarni `courier:position` oqimidan ko'rsatadi, "aloqa uzildi" (>2 daq signal yo'q) rangda belgilanadi. Kuryer rejimi — `CourierDeliveriesPage.vue` (bugungi marshrut, GPS'ni 15 soniyada bir marta `courier:location` orqali yuboradi, Screen Wake Lock — `navigator.wakeLock`, yangi bog'liqlik qo'shilmadi) + `CourierDeliveryDetailPage.vue` (naqd summasi bilan "Yetkazildi", natijada `acceptCode` ko'rsatiladi). `lib/socket.js` singleton qilib qayta ishlandi (`connectSocket()` endi bitta ulanishni qaytaradi, yangi `getSocket()`/`disconnectSocket()`) — xarita/kuryer sahifalari `AppLayout.vue`dagi bildirishnoma ulanishini qayta ishlatadi, ikkinchi Socket.IO ulanish ochilmaydi. Claude Browser'da barcha yangi sahifalar tekshirildi (Leaflet xaritasi Toshkent markazida to'g'ri render bo'ldi), konsolda xato yo'q, `pnpm build` muvaffaqiyatli.
- Task 7 (frontend, `apps/shop`) bajarildi — Faza 7ning oxirgi vazifasi. `socket.io-client`+`leaflet` birinchi marta qo'shildi, yangi `lib/socket.js` (apps/webdagi bilan bir xil singleton qolipi) `ShopLayout.vue`ga ulandi, `vite.config.js`ga `/socket.io` ws-proxy. `OrderDetailPage.vue` uch holatga qarab kengaytirildi: `shipped` — mini-Leaflet xarita (`courier:position`ni `courierMemberId` bo'yicha filtrlab), `delivered` — farqlar akti formasi (har item uchun `qtyAccepted`, kuryer aytgan kod), `accepted` — qaytarish formasi (item+miqdor). Claude Browser'da `window.fetch`ni vaqtincha almashtirib uch holatning hammasi (`shipped`/`delivered`/`accepted`) alohida-alohida render qilib tekshirildi — xaritasi, formalar to'g'ri ko'rindi, konsolda xato yo'q. Ikkala ilova `pnpm build` muvaffaqiyatli, butun repo lint 0 xato/format toza, backend 736/736 test yashil.
- **Faza 7 — 7/7 vazifa bajarildi.** Zakazni kuryerga biriktirish, kuryer PWA (marshrut+GPS+Wake Lock), jonli xarita (dispetcher — barcha kuryerlar, do'kon — o'z kuryeri), qabul qilish (acceptCode bilan farqlar akti) va qaytarish (vozvrat) — barchasi yozilgan va Claude Browser'da tekshirilgan. Yangi Prisma migratsiya kerak bo'lmadi (Faza 0'dan tayyor sxema). Farqlar akti/qaytarishning qarz (`debt_movements`) oqibati ataylab qoldirildi — `debts` moduli hali yo'q (Faza 8'ning birinchi vazifasi), tabiiy bog'liqlik ketma-ketligi. GPS retention global cron o'rniga tenant-ichi tozalash bilan yechildi (cross-tenant RLS-bypass Faza 11'gacha yo'q). Haqiqiy Postgres/Redis/GPS/Socket.IO real-vaqt oqimi hali tasdiqlanmagan (oldingi fazalardagi bilan bir xil infratuzilma cheklovi).
- Faza 8 (qarz — nasiya boshqaruvi) boshlandi: plan-rejimda kelishildi. Muhim topilma — `DebtMovement`/`Payment`/`PaymentAllocation` Prisma modellari va `debts.view`/`debts.manage` ruxsat kodlari Faza 0'dan tayyor edi (yangi migratsiya kerak emas), Faza 7'da yozilgan `accept()`/`returnItems()` esa JSDoc'da aynan shu nuqtada "Faza 8'ga qoldirildi" deb ochiq TODO qoldirgan edi. Arxitektura qarorlari: aging — `Order.dueDate`ni order-turdagi `DebtMovement`ga ko'chirib, barcha bir xil `orderId`li yozuvlarni netto qilish orqali (5 bucket: `notDue`/`0-15`/`16-30`/`31-60`/`60+`); kredit limit tekshiruvi `confirm()`da — `settings.creditLimitMode` (`block`/`warn`, `Company.settings` mavjud JSON ustuni, migratsiyasiz) + `debts.manage` bypass; to'lov taqsimlash — FIFO (eng eski `dueDate`) yoki qo'lda, ortiqcha to'lov `orderId:null` bilan alohida yoziladi (balans invarianti); akt sverki — `pdfmake` (standart PDFKit Helvetica shrifti, tashqi TTF fayl shart emas — Uzbek lotin apostrofi bilan to'liq ishlaydi), so'rov ichida sinxron (Excel export'dagi kabi, BullMQ'siz); kunlik eslatma — `companies` jadvali RLS bilan himoyalangan bo'lgani uchun (bypass roli Faza 11'gacha yo'q) kompaniyalar ro'yxati Redis'da yuritiladi (`lib/companies-registry.js`, yangi kompaniya `auth.service.js registerCompany()`da avtomatik qo'shiladi), `worker.js` har biri uchun alohida `withTenant` ochadi — Faza 7'dagi GPS-retention qarori bilan bir xil mantiq, lekin bu safar to'liq RLS-mos yechim (bypass shart emas). Realizatsiya (konsignatsiya) rejimi — DB sxema qo'llab-quvvatlamagani uchun (yangi migratsiya kerak) `BACKLOG.md`ga o'tkazildi, Faza 8'ning success test'ida qatnashmaydi.
- Task 1 (debts backend core) bajarildi: yangi `debts` moduli (repository — faqat `create`+o'qish, immutable jurnal; service — `getBalance`/`getStatement`/`getAging`/`createAdjustment`/`getMyBalance`/`getMyStatement`, ownership-vs-permission naqshi `orders.service.js`dagi bilan bir xil). Aging/netting mantig'i alohida `lib/debt-netting.js`ga chiqarildi (keyinroq `payments` moduli ham ishlatadi — moliyaviy hisob-kitob ikki joyda mustaqil yozilib chalkashib ketmasligi uchun). Yangi `CreditLimitExceededError` (`InsufficientStockError` naqshi). `seed.js`: `accountant` roliga birinchi marta ruxsat berildi (`debts.view`/`debts.manage`/`reports.view`), `warehouse_manager`ga `debts.view` qo'shildi. 34 yangi test, 787/787 backend yashil.
- Task 2 (kredit limit + qarz yozuvlari, `orders.service.js`ga qo'shimcha) bajarildi: `confirm()`ga `#assertCreditLimit()` — status tekshiruvidan keyin, stock-rezerv tsiklidan oldin (arzon tekshiruv birinchi). `accept()` — item tsiklida `debtTotal += price×qtyAccepted` yig'ib, oxirida bitta `type:"order"` yozuv. `returnItems()` — mavjud `docTotal` bilan bitta `type:"return"` yozuv (`amount:-docTotal`). Ikkalasi ham `orders.service.js`ning yangi `debtMovementsRepository`/`companiesRepository` bog'liqligi orqali, xuddi shu `withTenant` tranzaksiyasi ichida. 792/792 backend yashil.
- Task 3 (payments moduli) bajarildi: `PaymentsService.create()` — `debts.manage` talab qiladi, FIFO (`dueDate` bo'yicha, `null` oxirida) yoki qo'lda taqsimlash (`allocations` DTO'da berilsa), har ikkalasida ham ochiq qoldiqdan oshib ketish oldindan tekshiriladi. Izchil qaror: bu loyihada hech qanday joyda `SELECT...FOR UPDATE` ishlatilmaydi (Faza 3'da ham — atomik `upsert`+`{increment}` bilan yetarli edi), shuning uchun to'lov taqsimlashda ham xuddi shu darajadagi rigor saqlandi (yangi raw-SQL qulflash infratuzilmasi kiritilmadi) — bir xil kontragentga ikkita parallel to'lov nazariy race'ga ega, lekin bu mavjud kredit-limit tekshiruvi bilan bir xil, allaqachon qabul qilingan cheklov, yangi kiritilgan bo'shliq emas. 14 yangi test.
- Task 4 (aging report + akt sverki PDF) bajarildi: `GET /debts/aging` (Task 1'da tayyor edi, bu vazifada faqat frontend/PDF). PDF uchun `pdfmake` sinaldi — birinchi urinish (`pdfmake@0.3.11`, eng so'nggi versiya) `urlResolver`/fayl-tizim abstraksiyasi talab qildi (server uchun mos emas), `pdfmake@0.2.20`ga tushirildi. Muhim kashfiyot: PDFKit'ning standart 14 shrifti (`Helvetica` va h.k.) `pdfmake`ning `fonts` lug'atida to'g'ridan-to'g'ri font nomi sifatida ishlatilishi mumkin — tashqi TTF fayl (Roboto va h.k.) bundle qilish shart emas, Uzbek lotin matni (apostrof bilan) WinAnsiEncoding doirasida to'liq qo'llab-quvvatlanadi. `getStatement()`ning ichki mantig'i `#buildStatement()`ga chiqarildi — `getStatement()` va yangi `getStatementPdf()` bitta `withTenant` tranzaksiyasi ichida qayta ishlatadi (ikkita mustaqil tranzaksiya ochish o'rniga). 3 yangi test (jumladan haqiqiy PDF Buffer generatsiya qilib `%PDF` header tekshiruvi). 792/792 backend yashil.
- Task 5 (kunlik eslatma job) bajarildi: `lib/companies-registry.js` (Redis `SADD`/`SMEMBERS`) + `auth.service.js registerCompany()`ga ulash (tranzaksiyadan tashqarida, `domainEvents.emit` joylashuvi bilan bir xil o'rin). `apps/api/scripts/backfill-companies-registry.js` — mavjud kompaniyalar uchun bir martalik operator skripti. `queue.js`ga yangi `debt-reminders` navbati, `worker.js`ga repeat job (`0 8 * * *`, `jobId` bilan idempotent) + o'z alohida `NotificationsService` kompozitsiyasi (worker alohida process, `domainEvents` unga yetib bormaydi — `notifications.routes.js`dagi bilan bir xil naqsh). `NotificationsService.notifyDebtReminder()` — qabul qiluvchilar: `debts.manage` egalari + sotuv nuqtasiga biriktirilgan operator(lar) (`shop_operator`da ruxsat yo'qligi uchun to'g'ridan-to'g'ri userId bo'yicha), dublikat oldini olish `Notification.data` JSON-path filtri bilan (yangi migratsiyasiz). Yangi `SalePointsRepository.findByCounterpartyId()` (har kontragentga bitta sotuv nuqtasi — "kim javobgar" savoliga javob). 792/792 backend yashil (6 yangi).
- Task 6 (frontend, `apps/web`) bajarildi: `debts.api.js`/`payments.api.js`, `SalePointFormPage.vue`ga "Qarz holati" karta (mavjud "Operatorlar" karta naqshi, alohida Counterparty CRUD sahifasi yo'qligi sababli — sotuv nuqtasi = UI'dagi "mijoz"). `CounterpartyStatementPage.vue` — balans/harakatlar jadvali/PDF eksport/to'lov formasi (FIFO yoki qo'lda — ochiq order ro'yxati klient tomonda to'liq (filtrlanmagan) statement'dan hisoblanadi, yangi backend endpoint qo'shilmadi). `DebtsAgingPage.vue` — kompaniya bo'yicha jadval, muddati o'tgan qizil. Claude Browser'da `window.fetch` monkey-patch bilan uch sahifa ham tekshirildi (balans/statement/aging/qo'lda-taqsimlash ochiq-order ro'yxati to'g'ri hisoblandi), konsolda xato yo'q, `pnpm build` muvaffaqiyatli.
- Task 7 (frontend, `apps/shop`) bajarildi — Faza 8ning oxirgi vazifasi. `debts.api.js` (`getMyBalance`/`getMyStatement`), `MyDebtPage.vue` (balans muddati o'tgan bo'lsa qizil, davr filtri, tarix), pastki-nav'ga 4-band "Qarzim". Claude Browser'da tekshirildi — balans to'g'ri qizil rangda ko'rsatildi (mock ma'lumotda muddati o'tgan order bor edi), konsolda xato yo'q, `pnpm build` muvaffaqiyatli.
- **Faza 8 — 7/7 vazifa bajarildi.** Kredit limiti (block/warn), `debt_movements` jurnali (order/payment/return/adjustment), FIFO/qo'lda to'lov taqsimlash, aging hisoboti (5 bucket), akt sverki PDF (`pdfmake`, standart shrift), kunlik eslatma job (Redis-asoslangan kompaniyalar ro'yxati, RLS bypass'siz), ikkala frontend UI — barchasi yozilgan, testlangan (792/792 backend test), Claude Browser'da tekshirilgan. Yangi Prisma migratsiya kerak bo'lmadi (Faza 0'dan tayyor sxema). Realizatsiya (konsignatsiya) rejimi, to'liq approval-workflow, avans avtomatik hisoblash — `BACKLOG.md`ga o'tkazildi. Haqiqiy Postgres/Redis/BullMQ worker bilan hali tasdiqlanmagan (oldingi fazalardagi bilan bir xil infratuzilma cheklovi).

## Faza 9 — Kassa, valyuta, chop etish

- Faza 9 boshlandi: plan-rejimda kelishildi. Muhim topilma — `CashRegister`/`CashShift`/`ExpenseCategory`/`Transaction`/`ExchangeRate` Prisma modellari Faza 0'dan tayyor edi (yangi migratsiya kerak emas), `Payment.cashRegisterId`/`Transaction.paymentId` bog'lanishlari Faza 8'dan tayyor edi-yu, hech kim ishlatmagan edi. `orders.service.js create()` USD narxli mahsulotni hech qanday konvertatsiyasiz olib `order.currency`ni doim `"UZS"` deb qotirar edi — real bag, shu fazada tuzatildi. `companies` moduli umuman yo'q edi (faqat `create`/`findById` repository) — Faza 8'da o'qilgan `settings.creditLimitMode`ni yozadigan joy hech qachon yaratilmagan ekan, shu fazada birinchi marta to'ldirildi. Arxitektura qarorlari: kassa — bitta `cash` modul uch resurs (registers/expense-categories/transactions/shifts), `Transaction` immutable qilindi (`immutable.sql`); smena balansi `occurredAt` oynasi bilan hisoblanadi, yangi ustun kerak emas; to'lov→kassa bog'lanishi `PaymentsService`ga to'g'ridan-to'g'ri `transactionsRepository` orqali (servis→servis emas); valyuta konvertatsiyasi sotuv vaqtida (`lib/currency.js`, `computeQtyBase`dagi bilan bir xil "repository parametr sifatida" naqsh); chop etish `debts.pdf.js` naqshini kengaytirib, logo MinIO'dan base64 sifatida qo'shiladi.
- Task 1 (cash backend core) bajarildi: yangi `cash` moduli (`CashRegistersRepository`/`ExpenseCategoriesRepository`/`TransactionsRepository`, bitta `CashService`/`CashController`/`cash.routes.js`). Yangi `cash.view` ruxsat kodi (`cash.manage`dan tashqari, `debts.view`/`manage` naqshi bilan bir xil), `accountant` roliga ikkalasi ham berildi. `immutable.sql`ga `transactions` qo'shildi (pul jurnali ham `debt_movements`/`stock_movements` kabi tuzatilmaydi — faqat storno). 28 yangi test, 843/843 backend yashil.
- Task 2 (kun yopish + to'lov↔kassa) bajarildi: `CashShift` ochish/yopish — `expectedBalance = openingBalance + SUM(income/transfer_in) - SUM(expense/transfer_out)`, `[openedAt, hozir)` oralig'ida (yangi `shiftId` ustuni kerak emas). Bitta registerda bir vaqtda faqat bitta ochiq smena servis darajasida tekshiriladi. `payments.service.js`ga `cashRegistersRepository`/`transactionsRepository` DI qo'shildi — `dto.cashRegisterId` berilsa `Payment` yozilgandan keyin bitta `Transaction` (`type:"income"`) avtomatik yoziladi. Kassalar orasida ko'chirish — yangi `POST /cash/transfers` (bitta chaqiruvda ikkita yozuv: `transfer_out`+`transfer_in`, bir xil `occurredAt`). 51 yangi test, 864/864 backend yashil.
- Task 3 (valyuta) bajarildi: yangi `exchange-rates` moduli (`getCurrentRate` — avval kompaniya o'z kursi, bo'lmasa `exchangeRateMode:"manual"`da NotFound, `"cbu"`da CBU rasmiy kursiga tushadi; `setRate` — `upsert`, bir kunga bitta yozuv). `lib/currency.js` — `convertToUzs()` (pure) + `resolveExchangeRate()` (`computeQtyBase(tx, repo, ...)` naqshi bilan bir xil, repository parametr sifatida beriladi, servis servisga bog'lanmaydi). **Haqiqiy bag topildi va tuzatildi**: `orders.service.js create()` va `shop-catalog.service.js list()` USD narxni hech qachon konvertatsiya qilmas edi (test fixture'larida `currency` maydoni yo'qligi buni yashirib turgan edi) — endi `priceRow.currency !== "UZS"` bo'lsa kurs (lazy, faqat birinchi USD qatorda) olinib UZS'ga o'giriladi, kurs topilmasa `ValidationError`. CBU kunlik job — `worker.js`ga `processCbuRateJob()` (`cbu.uz` ochiq JSON API, native `fetch`, xato bo'lsa jim log qilib o'tkazib yuboriladi, oldingi kun kursi query fallback bilan ishlaydi), test yo'q (mavjud thumbnail/import/debt-reminder job'lari bilan bir xil — worker.js kod darajasida yoziladi, mahalliy ishga tushirilmaydi). 40 yangi test, 882/882 backend yashil.
- Task 4 (kompaniya brendingi) bajarildi: `companies` moduli birinchi marta to'liq (service/controller/routes/schemas) — avval faqat repository bor edi. `CompaniesRepository.update()` qo'shildi. `PATCH /companies/me` — `settings` **butunlay almashtirilmaydi**, mavjud kalitlar bilan birlashtiriladi (`{...existing, ...dto.settings}`) — aks holda faqat brend rangini o'zgartirish `creditLimitMode`ni tozalab qo'ygan bo'lardi. `POST /companies/me/logo` — `product-images.service.js`dagi MinIO naqshi bilan bir xil (`companies/{id}/logo.{ext}`, thumbnail kerak emas). 18 yangi test, 882/882 backend yashil.
- Task 5 (chop etish) bajarildi: yangi `printing` modul — `printing.pdf.js` (`renderOrderInvoicePdf`/`renderWarehouseDocPdf`, `debts.pdf.js`dagi bir xil `pdfmake`+standart Helvetica naqshi) + `printing.logo.js` (`loadCompanyLogoBase64` — MinIO'dan buffer o'qib base64 data URI'ga aylantiradi, xato/logo yo'q bo'lsa `null`, hujjat logo'siz davom etadi). `orders.repository.js`/`warehouse-docs.repository.js`ga yangi `findByIdForPrint()` metodlari (mahsulot nomi/artikuli+birlik qisqartmasi bilan include — mavjud `findById()` boshqa joylarda ishlatilgani uchun tegilmadi, alohida metod). MinIO/PDF generatsiya (tarmoq+CPU I/O) tranzaksiya tashqarisida — `product-images.service.js`dagi naqsh. `GET /orders/:id/invoice.pdf`, `GET /warehouse-docs/:id/act.pdf`. 19 yangi test (jumladan haqiqiy PDF Buffer, logo bilan/logosiz), 901/901 backend yashil.
- Task 6 (frontend: kassa + kompaniya sozlamalari, `apps/web`) bajarildi: `cash.api.js`/`companies.api.js`/`exchange-rates.api.js`. `CashRegistersPage.vue` (registerlar+xarajat kategoriyalari CRUD), `CashRegisterLedgerPage.vue` (tranzaksiyalar jadvali, smena ochish/yopish — ochiq smena bo'lsa yopish formasi, aks holda ochish formasi, yopilgan smenalar tarixi diff bilan; kassalar orasida ko'chirish formasi), `CompanySettingsPage.vue` (logo yuklash+önizleme, brend rangi, kredit limit/valyuta kursi rejimi, joriy USD kursini ko'rsatish+qo'lda kurs qo'yish). Nav'ga "Kassa"/"Sozlamalar" havolalari. Claude Browser'da uch sahifa ham `pinia._s.get('auth')`+`window.fetch` monkey-patch bilan tekshirildi, konsolda xato yo'q, `pnpm build` muvaffaqiyatli.
- Task 7 (frontend: chop etish + inkassatsiya, `apps/web`) bajarildi — Faza 9ning oxirgi vazifasi. `orders.api.js`/`warehouse-docs.api.js`ga `downloadInvoicePdf`/`downloadActPdf` (`debts.api.js downloadStatementPdf` naqshi — raw fetch+blob+avtomatik yuklab olish). `OrderDetailPage.vue`/`WarehouseDocFormPage.vue`ga "Chop etish" tugmasi (tegishli status'larda ko'rinadi). `DeliveryDetailPage.vue`ga "Kassaga topshirish" — kassa tanlab `cashCollected` summasini `Transaction(type:"income")` sifatida yozadi; **yangi "settled" flag/migratsiya yo'q** (rejalashtirilgan qaror) — qayta bosish xavfi `BACKLOG.md`ga qo'shildi. Claude Browser'da uchala oqim (chop etish tugmasi bosilganda xato yo'q, kassaga topshirish tugmasi bosilib "Kassaga topshirildi" xabari chiqishi) to'liq tekshirildi. `pnpm build` muvaffaqiyatli, butun repo lint 0 xato/format toza.
- **Faza 9 — 7/7 vazifa bajarildi.** Kassa/hisob raqamlar+tranzaksiyalar+xarajat kategoriyalari, kun yopish (smena, `expectedBalance`/`diff`), zakaz↔to'lov↔kassa bog'lanishi, valyuta (USD narxlar, CBU kunlik kurs, `orders`/`shop-catalog`dagi haqiqiy bag tuzatildi), kompaniya brendingi (logo+brend rangi, `companies` moduli birinchi marta), chop etish (nakladnaya+akt, logo bilan), ikkala frontend vazifa — barchasi yozilgan, testlangan (901/901 backend test), Claude Browser'da tekshirilgan. Yangi Prisma migratsiya kerak bo'lmadi (Faza 0'dan tayyor sxema). Kuryer inkassatsiya idempotency, to'liq valyutali qarz UI — `BACKLOG.md`ga o'tkazildi. Haqiqiy Postgres/Redis/MinIO/real CBU API javobi bilan hali tasdiqlanmagan (oldingi fazalardagi bilan bir xil infratuzilma cheklovi).

## Faza 10 — Dashboard va hisobotlar

- Faza 10 boshlandi: plan-rejimda kelishildi. Muhim topilma — `AuditLog` Prisma modeli va `immutable.sql`dagi trigger Faza 0'dan tayyor edi, lekin butun kod bazasida hech qayerda yozilmagan edi (tekshirildi). `debts.service.js getAging()` (Faza 8) kompaniya bo'yicha barcha kontragentlar balansini aging bucket bilan qaytaradi — bu aynan "qarzdorlik reestri" talabini qondiradi, yangi backend kerak bo'lmadi, frontend'da mavjud `debts-aging` sahifasiga havola yetarli bo'ldi. Arxitektura qarorlari: audit log — markazlashgan `lib/audit.js logAudit()` + tanlangan olti "yuqori-qiymatli" servis (butun kod bazasi emas — pul/qoldiq/hodim harakatlari), yangi `audit.view` ruxsati hech qaysi rolga berilmaydi (faqat owner ko'radi); hisobotlar — "sotilgan" = `status:"accepted"` + `qtyAccepted×price` (jo'natilgan emas, haqiqatda qabul qilingan — `debt_movements`dagi bilan bir xil mantiq), guruhlash service qatlamida JS `reduce` bilan (repository `groupBy` ishlatmaydi); marja — joriy o'rtacha tannarx, `stock.service.js averageCost()`dan `lib/inventory-cost.js`ga chiqarilgan umumiy hisoblash (`debt-netting.js` naqshi); sklad aylanmasi — joriy qoldiqni o'rtacha sifatida ishlatib (vaqt-og'irlikli o'rtacha emas, MVP soddalashtirish); dashboard — `debts`/`stock`/`cash` **servislariga emas, repository'lariga** bog'lanadi (servis servisga bog'lanmaydi qoidasi) — `getAging()`ning ichki mantig'ini takror ishlatadi; standart bosh sahifa o'zgarmaydi, `dashboard` alohida yangi route.
- Task 1 (audit log backend core) bajarildi: yangi `audit-logs` moduli (repository — `create`/`list`, immutable — faqat yozish/o'qish; service/controller/routes, `GET /audit-logs` `audit.view` bilan himoyalangan). `lib/audit.js` — `logAudit(tx, auditLogsRepository, data)` (`resolveExchangeRate(tx, repo, ...)` Faza 9'dagi bilan bir xil "repository parametr sifatida beriladi" naqshi — yangi servis klassi emas). `seed.js`ga yangi `audit.view` ruxsat kodi qo'shildi, hech qaysi `ROLE_PERMISSIONS`ga berilmadi (faqat owner). 8 yangi test, 909/909 backend yashil.
- Task 2 (audit log instrumentatsiya) bajarildi: olti servisga (`orders.service.js` — `confirm`/`cancel`/`accept`/`returnItems`; `warehouse-docs.service.js` — `confirm`/`cancel`; `payments.service.js` — `create`; `debts.service.js` — `createAdjustment`; `cash.service.js` — `createTransaction`/`transfer`/`closeShift`; `company-members.service.js` — `create`/`update`/`resetPassword`) `auditLogsRepository` DI qo'shildi, har mutatsiyadan keyin mavjud `withTenant` tranzaksiyasi ichida `logAudit()` chaqiriladi. Har servisning routes.js kompozitsiya ildizi ham yangilandi. Bitta texnik nuqta: `warehouse-docs.invariant.test.js` (xotiradagi fake DB bilan REAL service kodini sinaydigan Faza 3'dan qolgan test) `auditLog.create` fake metodisiz 2 ta test buzilib qoldi — `createFakeDb()`ga oddiy stub qo'shib tuzatildi. 909/909 backend yashil (yangi assertion'lar qo'shilgan holda).
- Task 3 (hisobotlar: sotuv + mahsulot/marja) bajarildi: `lib/inventory-cost.js` — `computeAverageCost(movements)` pure funksiya `stock.service.js averageCost()`dan chiqarildi (o'zi ham shu funksiyani ishlatadigan qilib qayta yozildi). Yangi `OrdersRepository.listAcceptedForReports(tx, companyId, {from,to})` — `status:"accepted"` zakazlar, item'lar mahsulot nomi/artikuli bilan include (`findByIdForPrint()`dagi bilan bir xil "alohida maxsus metod, umumiy `findById()`ga tegilmaydi" qarori). Yangi `reports` moduli — `GET /reports/sales` (kunlik dinamika, JS `Map` bilan sana bo'yicha guruhlash), `GET /reports/products` (top mahsulotlar daromad bo'yicha + har biri uchun `computeAverageCost()` bilan marja, `limit` parametri bilan qisqartiriladi). 15 yangi test.
- Task 4 (hisobotlar: sklad aylanmasi + dashboard) bajarildi: `StockMovementsRepository.listByPeriod()` (barcha harakatlar, kirim ham chiqim ham — `listPositiveWithCost()`dan farqli). `ReportsService.getStockTurnover()` — chiqim (`qty<0`) harakatlarni mahsulot bo'yicha yig'ib, joriy qoldiq×joriy o'rtacha tannarx bilan o'rtacha qiymatni yaqinlashtiradi, aylanma koeffitsientini hisoblaydi. `getDashboard()` — `debts`/`stock`/`cash` **repository'lariga** to'g'ridan-to'g'ri bog'lanib (servis-servisga bog'lanmaydi qoidasi saqlangan holda) bugungi sotuv+kutilayotgan zakazlar soni (`OrdersRepository.countPending()` — yangi, `status in [new,confirmed,picking]`)+kassa qoldig'i (valyuta kesimida netto)+qarzdorlik (jami+muddati o'tgan, `computeOpenOrderBalances()` qayta ishlatildi)+tugayotgan mahsulotlar sonini birlashtiradi. 22 yangi test, 930/930 backend yashil.
- Task 5 (frontend: Dashboard, `apps/web`) bajarildi: `echarts`+`vue-echarts` birinchi marta o'rnatildi (yangi `lib/echarts-setup.js` — `use([CanvasRenderer, LineChart, BarChart, ...])` markazlashgan, ikkita chart-sahifa buni qayta ishlatadi). `reports.api.js`, `DashboardPage.vue` — kartalar (bugungi sotuv/kutilayotgan zakazlar/kassa/qarz/muddati o'tgan/tugayotgan mahsulotlar) + 30 kunlik sotuv dinamikasi `VChart` grafigi. Nav'ga "Dashboard" birinchi havola sifatida qo'shildi (standart bosh sahifa `products`ligicha qoladi — `reports.view` bo'lmagan rollar uchun xavfsiz). Claude Browser'da tekshirildi — kartalar to'g'ri qiymat bilan render bo'ldi, ECharts grafigi tooltip bilan ishladi (skrinshot orqali tasdiqlandi), konsolda xato yo'q, `pnpm build` muvaffaqiyatli (yangi chunk-hajmi ogohlantirishi — kutilgan, ECharts katta kutubxona, faqat shu route lazy-load qilinganda yuklanadi).
- Task 6 (frontend: hisobotlar, `apps/web`) bajarildi: `SalesReportPage.vue` (davr filtri + bar/line grafik + jadval, jami summasi), `ProductsReportPage.vue` (top mahsulotlar jadvali — daromad/tannarx/marja/marja% ustunlari, `null` qiymatlar "—" bilan ko'rsatiladi), `StockTurnoverPage.vue` (chiqim/o'rtacha qoldiq/aylanma jadvali, yaqinlashtirish haqida izoh). Uch sahifa o'zaro navigatsiya havolalari bilan bog'langan, qarzdorlik reestri uchun mavjud `debts-aging` sahifasiga havola (yangi sahifa yaratilmadi). Nav'ga "Hisobotlar" havolasi (sotuv sahifasiga ochiladi). Claude Browser'da uchala sahifa tekshirildi, konsolda xato yo'q, `pnpm build` muvaffaqiyatli.
- Task 7 (frontend: audit log UI, `apps/web`) bajarildi — Faza 10ning oxirgi vazifasi. `audit-logs.api.js`, `AuditLogListPage.vue` — obyekt turi/sana filtri, jadval (sana/foydalanuvchi/amal/obyekt), har qatorda "Tafsilot" tugmasi `before`/`after` JSON'ni kengaytirib ko'rsatadi (`reactive` obyekt bilan qator-darajasidagi ochish/yopish holati). Nav'ga "Audit log" havolasi. Claude Browser'da tekshirildi — filtr select'i to'g'ri variantlar bilan render bo'ldi, "Tafsilot" tugmasi bosilganda `before`/`after` to'g'ri formatlangan JSON ko'rsatdi, konsolda xato yo'q, `pnpm build` muvaffaqiyatli, butun repo lint 0 xato/format toza.
- **Faza 10 — 7/7 vazifa bajarildi.** Audit log (markazlashgan yordamchi + olti yuqori-qiymatli servis, to'liq qamrov emas), hisobotlar backend (sotuv dinamikasi, top mahsulotlar+marja, sklad aylanmasi, dashboard agregat endpoint'i), qarzdorlik reestri (Faza 8'dagi mavjud aging'ga havola), ikkala frontend vazifa (Dashboard — ECharts birinchi marta, uchta hisobot sahifasi, audit log UI) — barchasi yozilgan, testlangan (930/930 backend test), Claude Browser'da tekshirilgan (jumladan grafik render va JSON tafsilot ochish). Yangi Prisma migratsiya kerak bo'lmadi (Faza 0'dan tayyor `AuditLog` sxema). Audit log to'liq qamrov, `req.ip` yozish, tarixiy tannarx bilan aniqroq marja/aylanma hisobi — `BACKLOG.md`ga o'tkazildi. Haqiqiy Postgres/Redis bilan hali tasdiqlanmagan (oldingi fazalardagi bilan bir xil infratuzilma cheklovi).

## Faza 11 — Landing, vitrina, super-admin

- Faza 11 boshlandi: plan-rejimda kelishildi (`Agent` tadqiqot orqali auth ichki tuzilishi o'rganildi). Muhim topilma — `Company.slug`/`showcaseSettings` (jsonb — "yoqilgan/narx turi/kategoriya"), `Lead` modeli, `Subscription` modeli Faza 0'dan tayyor edi, lekin hech qayerda ishlatilmagan; platform-admin konsepsiyasi kod bazasida umuman yo'q edi (JWT payload doim `companyId` talab qiladi — `req.auth` har doim bitta kompaniyaga bog'langan). Arxitektura qarorlari: vitrina — Vue SSR emas, `apps/api`da yengil HTML-render endpoint (template literal, meta/OG/Schema.org + server-render mahsulot ro'yxati, `withoutTenant`+qo'lda `companyId` filtri — slug orqali company_id hali noma'lum yagona qonuniy holat); super-admin — yangi `User.isPlatformAdmin` + alohida `platform_access` token turi (refresh-token/sessiya ataylab yo'q, 12soat TTL — MVP soddalashtirish), `apps/web` qobig'ida mustaqil auth filiali (yangi Docker servis emas); `apps/landing` — `vite-ssg` bilan birinchi marta quriladi, `vue-i18n` (uz/ru) loyihada birinchi marta.
- Task 1 (vitrina backend) bajarildi: `showcase` moduli — repository (faqat `createLead`, qolgan o'qishlar mavjud `companies`/`products`/`product-prices`/`product-images`/`price-types` repositorylaridan qayta ishlatiladi, `ShopCatalogService` bilan bir xil "repository-only aggregatsiya" naqshi), `showcase.html.js` (sof funksiya, `debts.pdf.js` naqshi — DB'ni bilmaydi, to'g'ridan-to'g'ri unit test qilinadi), `GET /:slug` (to'liq HTML, Googlebot uchun JS shart emas) + `POST /:slug/leads` (rate-limit bilan). Ishlab chiqarish paytida ikkita xato topildi va tuzatildi: JSON-LD `<script>` ichiga qochirilmagan kompaniya nomi XSS ochib qo'yardi (`</script><script>` bilan chiqib ketish mumkin edi) — `<` almashtirish bilan tuzatildi; `notifications.service.js`ga yangi `notifyLeadNew()` — `lead.new` hodisasi, `companies.manage` egalariga (PLAN.md'ning "lid egaga tushadi" talabini qondiradi). 22 yangi test.
- Task 2 (super-admin backend) bajarildi: `User.isPlatformAdmin` (yangi migratsiya — Faza 0'dan beri uchinchi haqiqiy yangi ustun, `PushSubscription`/`isPlatformAdmin`dan keyin), `lib/jwt.js`ga `signPlatformAccessToken()`. `platform-auth` moduli — login (`withoutTenant`, mavjud `LoginAttemptsRepository` qayta ishlatildi, `platform:` prefiksi bilan alohida kalit fazasi). `platform` moduli — `listCompanies`/`getCompany`/`updateSubscription` (cross-tenant, `requirePlatformAdmin` orqasida). `scripts/create-platform-admin.js` — bir martalik operator skripti (`backfill-companies-registry.js` naqshi). 32 yangi test.
- Task 3 (vitrina frontend sozlamalari, `apps/web`) bajarildi: `CompanySettingsPage.vue`ga yangi "Vitrina" karta (yoqish/o'chirish, slug, narx turi, kategoriya) — mavjud `companies.service.js updateMe()` kengaytirildi (`showcaseSettings` ham `settings`dagi kabi mavjud kalitlar bilan birlashtiriladi, slug uniqueness tekshiruvi bilan `ConflictError`).
- Task 4 (super-admin frontend, `apps/web`) bajarildi: `usePlatformAuthStore` — kompaniya `authStore`dan **butunlay mustaqil** (alohida token xotirasi, refresh yo'q), `platformApiFetch` (alohida client), `PlatformLoginPage.vue`/`PlatformLayout.vue`/`PlatformCompaniesPage.vue` (ro'yxat+qidiruv+qatordan kengaytirilgan obuna tahrirlash formasi). Router'ga `/platform/login`+`/platform/*` filiali — `beforeEach` guard'ga alohida platform-filiali qo'shildi, mavjud company-auth filialiga tegilmadi.
- Task 5 (`apps/landing` skeleton) bajarildi: `vite-ssg`+Vue+Tailwind+`vue-i18n` birinchi marta o'rnatildi. Muhim texnik nuqta: `@unhead/vue`ning ikkita versiyasi (`vite-ssg`ning ichki `^2.1.2` va mening avval qo'shgan `^3.1.7`im) alohida modul instansiyasi yaratib, `useHead()` yozgan holat `vite-ssg`ning SSR render kontekstiga yetib bormasdi — versiyani `^2.1.2`ga tushirib pnpm bitta instansiyaga deduplikatsiya qilgach tuzatildi. `vite-ssg build` bilan tasdiqlandi — `dist/index.html`da server-render matn to'g'ridan-to'g'ri ko'rindi.
- Task 6 (`apps/landing` kontent + SEO + deploy) bajarildi: bosh sahifa (hero/6 imkoniyat/4 tarif/footer, uz/ru to'liq tarjima) — **tariflarda real so'm narxi yo'q** (biznes hali narx belgilamagan, xayoliy raqam yozish noto'g'ri bo'lardi — `pricing.cta`="Bog'lanish"). `useHead()` bilan meta/OG/canonical/`Organization` JSON-LD; ikkinchi topilgan xato — JSON-LD uchun `children` emas, `innerHTML` kaliti kerak edi (`children` HTML atributi sifatida chiqib ketardi). `scripts/generate-seo.js` — build'dan keyin `sitemap.xml`/`robots.txt` yozadi (`src/routes.js`dan avtomatik). `Dockerfile` (statik build+nginx, `apps/web`dan farqli — bu yerda production-style, chunki SEO'ning butun maqsadi server-render HTML), `docker-compose.yml`ga `landing` servisi (port 4173, asosiy nginx'ning `/` marshrutiga hali ulanmagan — haqiqiy subdomen ajratish Faza 12).
- Task 7 (Claude Browser + build tekshiruvi) bajarildi: `apps/landing` dev-server'da hero/imkoniyatlar/tariflar to'g'ri render bo'ldi, RU tugmasi bosilganda butun sahifa (jumladan `<title>`) ruscha almashdi. `apps/web`da (mocked auth+fetch) vitrina karta to'g'ri qiymatlar bilan render bo'ldi, saqlash to'g'ri `PATCH` payload yubordi; super-admin login→kompaniyalar ro'yxati→tahrirlash→saqlash oqimi to'liq ishladi (to'g'ri `PATCH /platform/companies/:id/subscription`). `showcase.html.js`ning natijasi to'g'ridan-to'g'ri (loopback HTTP server orqali) brauzerda ochilib tekshirildi — katalog kartalar, narx formatlash, va lid formasi (JS orqali `fetch`, muvaffaqiyat xabari, forma tozalanishi) to'g'ri ishladi.
- **Faza 11 — 8/8 vazifa bajarildi.** Vitrina (server-render HTML, autentifikatsiyasiz `showcase` moduli, lid→bildirishnoma), super-admin (`isPlatformAdmin`+alohida token turi+`apps/web`dagi mustaqil auth filiali), `apps/landing` (`vite-ssg`, uz/ru, SEO meta/OG/JSON-LD/sitemap, Docker deploy) — barchasi yozilgan, testlangan (981/981 backend test, +54 yangi), Claude Browser'da uch yo'nalish ham tekshirilgan. Yangi Prisma migratsiya — `User.isPlatformAdmin` (qo'llash keyingi bosqichda, infratuzilma cheklovi). Self-serve ro'yxatdan o'tish sahifasi yo'qligi, super-admin refresh-oqimi yo'qligi, real subdomen routing yo'qligi — `BACKLOG.md`ga o'tkazildi.
- `main`ga push qilishdan oldin `/code-review` (8 finder + 4 verifier agent, high effort) ishga tushirildi va 6 ta tasdiqlangan muammo shu faza doirasida tuzatildi: `showcase.service.js getShowcase()`ning N+1 so'rov+MinIO chaqiruvlari `withoutTenant` tranzaksiyasi ichida ketma-ket bajarilishi (ochiq, autentifikatsiyasiz sahifada Prisma standart 5s tranzaksiya muddatini oshirib yuborishi mumkin edi) — batch so'rovlar (`ProductPricesRepository.listCurrentByProducts()`/`ProductImagesRepository.listByProducts()`, yangi) DB tranzaksiyasi ichida, MinIO presign chaqiruvlari `Promise.all` bilan tashqarida va parallel qilib qayta qurildi; `/platform` (bola-marshrutsiz) bo'sh sahifa ko'rsatishi — `redirect` bilan `platform-companies`ga tuzatildi; `platform-client.js`da 401'dan keyin qayta yo'naltirish yo'qligi (foydalanuvchi "o'lik" holatda qolib ketardi) — dinamik `router` importi bilan `/platform/login`ga yo'naltirish qo'shildi; `showcase`/`platform-auth` modullarida CLAUDE.md'ning "har modulda `<nomi>.schemas.js`" qoidasi buzilgani — ikkala moduliga ham yangi schemas.js qo'shildi. Qolgan 4 ta tasdiqlangan/ehtimoliy topilma (auth middleware/lockout kod takrorlanishi, `PRESIGNED_URL_TTL_SECONDS` uch nusxa, `withoutTenant`ning RLS ikkinchi qavatini chetlab o'tishi — ataylab va hujjatlashtirilgan, companies/auth'dagi umumiy check-then-act race naqshi) — `BACKLOG.md`ga yozildi (kichik xavf, kod bazasining boshqa joylarida ham qabul qilingan naqsh). 986/986 backend test yashil (+5 yangi), lint/format toza, `apps/web` build muvaffaqiyatli.

## Faza 12 — Sayqal va ishga tushirish (kod qismi)

- Faza 12 boshlandi: plan-rejimda kelishildi (3 Explore agent + 1 Plan agent tadqiqot). Foydalanuvchi bilan aniq kelishilgan chegara — real domenga SSL/CD deploy va 2-3 haqiqiy biznes bilan pilot shu sessiyadan tashqarida (real server/kredensial/mijoz kerak). Muhim topilmalar: `apps/web`da ro'yxatdan o'tish sahifasi umuman yo'q edi (backend `POST /auth/register` Faza 0'dan tayyor, frontend yo'q); `apps/web`/`apps/shop`da `vue-i18n` umuman yo'q (`apps/landing` — Faza 11'dan yagona namuna); `apps/shop`da offline/IndexedDB kodi yo'q, lekin `createOrderSchema`da mijoz-generatsiya qiluvchi `idempotencyKey` allaqachon bor (offline replay uchun tayyor poydevor); `apps/web/src/sw.js` faqat Web Push, precaching o'chirilgan edi. Arxitektura qarorlari: offline navbat — SW Background Sync emas (Safari/iOS qo'llab-quvvatlamaydi), ilova darajasidagi IndexedDB outbox; i18n — avval infratuzilma, keyin ilova bo'yicha to'liq qamrov (mexanik ko'p-fayl o'zgartirish, Faza 10 naqshi); onboarding checklist — hisoblanadigan (yangi DB ustuni yo'q); demo-rejim — mavjud repositorylar orqali urug'lanadi; prod deploy — faqat kod/konfig.
- Task 1 (i18n infratuzilma) bajarildi: `vue-i18n` `apps/web`+`apps/shop`ga qo'shildi, `apps/landing` naqshi bo'yicha `src/i18n/{index.js,uz.json,ru.json}` + `main.js`da `localStorage`ga saqlanadigan til tanlovi. Til almashtirgich tugmasi ikkala ilova header'iga qo'shildi. Claude Browser'da tekshirildi (RU↔UZ almashish to'g'ri ishladi).
- Task 2 (i18n `apps/web` to'liq) bajarildi: 36 fayl (platform-admin sahifalari ataylab chiqarib tashlandi — ichki vosita, mijozga ko'rinmaydi) 7 parallel agentga bo'lib berildi, har biri o'z JSON chunk'ini alohida yozdi (race condition oldini olish uchun asosiy `uz.json`/`ru.json`ga to'g'ridan-to'g'ri emas), keyin qo'lda birlashtirildi. Claude Browser'da bir necha sahifa (jumladan status-map'lar `computed()`ga aylantirilgan `Orders`) ikkala tilda tekshirildi.
- Task 3 (i18n `apps/shop` to'liq) bajarildi: 9 fayl kichik hajmi tufayli to'g'ridan-to'g'ri (agentsiz) bajarildi. Claude Browser'da tekshirildi.
- Task 4 (offline outbox `apps/shop`) bajarildi: `lib/offline-outbox.js` (IndexedDB, `idb`), `CartPage.vue onCheckout()` — offline/tarmoq xatosida navbatga qo'yadi (mavjud `idempotencyKey` bilan), `ShopLayout.vue`da `online` hodisasi+mount+60s interval flush+navbat badge. **Xato topildi va tuzatildi**: Claude Browser sinovida Vite proxy backend ishlamaganda 5xx HTTP javob qaytarishi (haqiqiy tarmoq xatosi emas, lekin `ApiError` sifatida talqin qilingan) navbatdan noto'g'ri o'chirib yuborar edi — `flushOutbox()` endi faqat 4xx (haqiqiy rad javob) o'chiradi, 5xx/tarmoq xatosi saqlab qoladi (bu productionda ham to'g'ri: nginx 502 xuddi shunday ko'rinadi).
- Task 5 (offline outbox `apps/web`) bajarildi: `sw.js` precaching faollashtirildi (`workbox-precaching`, `injectionPoint: undefined` olib tashlandi — endi 74+ element keshlanadi). `lib/offline-outbox.js` (bir xil naqsh, IndexedDB `apps/shop` bilan mustaqil) — sklad hujjatini tasdiqlash/bekor qilish uchun, `id = docId:action` bilan dedupe. Amal turi tanlovi ataylab: `confirm()`/`cancel()` allaqachon holat-himoyasiga ega (`ConflictError` — qayta urinish tabiatan xavfsiz), yangi `idempotencyKey`/migratsiya kerak bo'lmadi (hujjat yaratish esa idempotency'siz, offline navbatga qo'yilmadi — dublikat xavfi bo'lardi).
- Task 6 (onboarding: register+demo+checklist) bajarildi: `RegisterPage.vue`, `registerSchema.demo`, `registerCompany()` — `demo:true` bo'lsa mavjud repositorylar orqali (xom SQL emas) 1 sklad+5 mahsulot (`unitsRepository.list()`dan "dona" birligi topilib)+1 mijoz+1 narx turi+1 sotuv nuqtasi urug'laydi (ochilish qoldig'i ataylab urug'lanmadi — `WarehouseDocsService.confirm()` ichki `withTenant` ochib nested-tranzaksiya muammosi yaratardi, demo uchun bu murakkablikka arzimaydi). `OnboardingChecklist.vue` — hisoblanadigan (sklad/mahsulot/sotuv-nuqta/zakaz soni mavjud GET endpoint'lardan), Dashboard'ga joylashtirilgan. **Yo'lda topilgan jiddiy bo'shliq**: `apps/web`da sklad qo'shish sahifasi umuman yo'q edi (backend CRUD tayyor, frontend yo'q) — onboarding checklist'ning birinchi bosqichi buni talab qilgani uchun `WarehouseListPage.vue` (yaratish+ro'yxat) shu vazifa doirasida qo'shildi. Backend: 3 yangi test, 989/989. Claude Browser'da to'liq oqim (ro'yxatdan o'tish→dashboard→sklad qo'shish→checklist yangilanishi) tekshirildi.
- Task 7 (prod deploy konfiglari) bajarildi: `docker-compose.prod.yml` (postgres/redis/minio portlari faqat ichki tarmoqqa, `web`/`shop`/`landing` statik build+nginx image'lariga GHCR prefiksi bilan bog'langan — `docker compose pull` ishlashi uchun), `nginx/nginx.prod.conf` (4 subdomen+SSL+certbot webroot, bootstrap tartibi izohda hujjatlashtirilgan), `apps/shop/Dockerfile`+`apps/web/Dockerfile.prod` (`apps/landing`dagi build+nginx naqshi, SPA fallback uchun alohida `nginx.spa.conf`), `.github/workflows/deploy.yml` (GHCR build+push — `GITHUB_TOKEN` bilan haqiqatan ishlaydi; SSH deploy — faqat `SSH_HOST` sekret sozlansa ishga tushadi), `scripts/backup.sh` (pg_dump+MinIO mirror+rotatsiya), Uptime Kuma xizmati. Barcha YAML fayl sintaksisi `js-yaml` bilan tekshirildi; real serverga qarshi ishga tushirilmagan (kredensial/server yo'q, foydalanuvchi bilan kelishilgan chegara).
- Task 8 (xavfsizlik auditi + yakun) bajarildi — Faza 12ning oxirgi vazifasi. Kod darajasidagi OWASP-uslubidagi audit (haqiqiy pentest emas): CORS ochiq edi (`cors()` — barcha origin) → `APP_WEB_URL`/`APP_SHOP_URL`/`PUBLIC_BASE_URL` oq ro'yxatiga o'tkazildi; `POST /auth/register` rate-limit'siz edi (Task 6'da haqiqiy frontend bilan ulandi — ommaviy soxta ro'yxatdan o'tish xavfi) → IP bo'yicha 10/soat limit qo'shildi; `pnpm audit` bitta moderate zaiflik topdi (`uuid@<11.1.1`, `exceljs` orqali transitiv) → `pnpm-workspace.yaml overrides` bilan tuzatildi, `pnpm audit` endi 0 zaiflik. Ko'rib chiqilgan va to'g'ri topilgan: cookie flags (httpOnly/secure/sameSite/domain), brute-force lockout, SQL in'ektsiya (Prisma parametrlashtirilgan), XSS (`v-html` 0 ta, `showcase.html.js`da `escapeHtml()` to'g'ri qo'llanilgan), mass assignment (Zod DTO), multi-tenant izolatsiya, CSRF. Barchasi `SECURITY.md`ga yozildi. 988/988 backend test yashil (uuid bump'dan keyin ham, exceljs export/import testlari alohida tekshirildi), butun repo lint 0 xato/format toza, uchala frontend (`web`/`shop`/`landing`) build muvaffaqiyatli.
- **Faza 12 (kod qismi) — 8/8 vazifa bajarildi.** i18n (ikkala asosiy ilova to'liq uz/ru), PWA offline+background sync (ilova darajasidagi IndexedDB outbox, ikkala ilovada — real xato topilib tuzatilgan holda), onboarding (demo-rejim+checklist, yo'lda topilgan sklad-sahifasi bo'shlig'i bilan birga to'ldirildi), prod deploy konfiglari (docker-compose/nginx/Dockerfile/CI — yozilgan, real serverga sinovdan o'tkazilmagan), xavfsizlik auditi (3 real topilma tuzatildi, qolgani hujjatlashtirilgan) — barchasi yozilgan, testlangan (988/988 backend test), Claude Browser'da tekshirilgan. Real domenga SSL/CD deploy va real biznes pilot — ataylab qamrovdan tashqarida (foydalanuvchi bilan kelishilgan), `BACKLOG.md`/`SECURITY.md`da hujjatlashtirilgan. Yangi Prisma migratsiya kerak bo'lmadi.
