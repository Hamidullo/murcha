# @murcha/shop

Do'kon uchun B2B zakaz PWA (shop.murcha.uz), `apps/web`dan mustaqil, yengil. Vue 3.5 + Vite + `vite-plugin-pwa`. Auth — `apps/web` bilan bir xil (bitta backend API). `pnpm --filter @murcha/shop dev` — port 5174.
